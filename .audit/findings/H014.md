# H014: Narrowing Cast Safety on v2 (10-Player Pot Scaling)

**Status:** NOT_VULNERABLE | **Severity:** N/A | **Date:** 2026-05-07

## One-Line Summary
u128 → u64 narrowing at v2 `settle_match:418` remains safe under 10-player pot ceiling (100 SOL × 10 = 10^12 lamports ≪ u64::MAX 1.8×10^19).

## Technical Details

### Narrowing Cast in Question
**File:** `programs/solshot-escrow-v2/src/lib.rs:418`  
**Operation:** `let total_pot_128: u128 = (wager_lamports as u128) * (num_deposited as u128);`  
→ `total_pot_128 as u64`

### Pot Ceiling Calculation (v2)
- MAX_WAGER: 100 SOL = 10^11 lamports
- MAX_PLAYERS: 10 (u16 deposits_mask allows 16 bits, capped at 10)
- **Max pot:** 100 SOL × 10 = 10^12 lamports = 1,000,000,000,000 lamports

### Cast Safety Validation
- u64::MAX = 1.8 × 10^19 lamports
- u64::MAX / max_pot = 1.8 × 10^19 / 10^12 = 1.8 × 10^7
- **Headroom factor:** 18 million ✓

**Conclusion:** The narrowing from u128 to u64 is lossless. No bits are lost; no truncation occurs. Feb H019 dismissal (v1) extends unchanged to v2.

### Defense Layers
1. **u128 widening** (line 413): prevents intermediate overflow during multiplication
2. **Bounded inputs** (v2:34-35): MAX_WAGER, MAX_PLAYERS constants enforced at `create_match`
3. **Runtime checks** (v2:201-214): `require!(wager >= MIN_WAGER)`, `require!(players.len <= max_players)`

## Verification Method
Arithmetic audit (02-arithmetic.md A06):  
"v2's `start_with_depositors` pot calc is safe — max product = 10 × MAX_WAGER = 1T lamports = 1e12 < u64::MAX (1.8e19)."

## Related Findings
- **A06** (Arithmetic, CONDENSED): Confirms same math under 10-player scale ✓
- **H019** (Feb, dismissed): Original 4-player narrowing safety — extends to 10-player unchanged
- **H028** (Feb, invalidated on v2): BPS configuration attack — separate risk, unrelated to this cast

## Recommendation
**No action required.** Document in Phase 4 finalization only.
