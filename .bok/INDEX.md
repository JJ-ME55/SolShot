# Book of Knowledge — Math Region Index

Generated by /BOK:scan on 2026-02-23.

## Summary
- Files scanned: 1
- Files with math signals: 1
- Math regions identified: 7
- Kani available: no (Windows — Linux/macOS only; degraded mode)
- GL docs available: no
- SOS findings available: yes (BATCH-02 arithmetic, BATCH-04 economic — off-chain focus)

---

## Math Regions

### Fee Calculations

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `settle_match` (lines 228-305) | `programs/solshot-escrow/src/lib.rs` | Simple | 18 | no | yes (H016 — off-chain float rounding; on-chain uses u128 BPS) |

**Details:**
- u128 widening: `(wager_lamports as u128).checked_mul(2)` — total pot in u128
- Treasury fee: `(total_pot_128 * TREASURY_BPS as u128) / BPS_DENOMINATOR as u128` then cast to u64
- Ops fee: `(total_pot_128 * OPS_BPS as u128) / BPS_DENOMINATOR as u128` then cast to u64
- Winner remainder: `total_pot - treasury_amount - ops_amount` (avoids dust loss)
- **Critical invariant:** `winner_amount + treasury_amount + ops_amount == total_pot` (conservation of value)
- **Rounding concern:** Integer division truncation in BPS math — need to prove no lamport dust is lost or created
- **Overflow concern:** `wager_lamports as u128 * 2` — safe for u128, but `total_pot_128 as u64` narrowing needs proof
- Constants: TREASURY_BPS=700, OPS_BPS=300, BPS_DENOMINATOR=10000 → 7% + 3% = 10%, winner gets 90%

### Pot Calculation

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `deposit_wager` (lines 156-223) | `programs/solshot-escrow/src/lib.rs` | Simple | 3 | no | no |

**Details:**
- `wager.checked_mul(2)` — u64 overflow check for total_pot event field
- **Invariant:** total_pot == 2 * wager_lamports (no partial deposits possible)
- Bounded by MAX_WAGER_LAMPORTS (100 SOL = 100_000_000_000) — `100B * 2 = 200B` fits u64 (max ~18.4E18)

### Timestamp / Duration

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `settle_match` deadline (lines 236-244) | `programs/solshot-escrow/src/lib.rs` | Simple | 2 | no | no |
| `cancel_match` timeout (lines 329-333) | `programs/solshot-escrow/src/lib.rs` | Simple | 2 | no | no |
| `permissionless_reclaim` timeout (lines 404-410) | `programs/solshot-escrow/src/lib.rs` | Simple | 2 | no | no |

**Details:**
- `activated_at.checked_add(SETTLEMENT_TIMEOUT_SECONDS)` — i64 overflow check (3600s)
- `timeout_reference.checked_add(TIMEOUT_SECONDS)` — i64 overflow check (86400s)
- `timeout_reference.checked_add(PERMISSIONLESS_RECLAIM_TIMEOUT)` — i64 overflow check (172800s)
- **Invariant:** `activated_at + timeout < i64::MAX` for any valid unix timestamp
- Solana clock max is ~year 2262 (i64 max / seconds), so overflow is theoretical but BOK should still prove it

### Wager Bounds Validation

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `create_match` (lines 110-152) | `programs/solshot-escrow/src/lib.rs` | Simple | 4 | no | yes (H011 — off-chain negative wager; on-chain uses u64 — no negative possible) |

**Details:**
- MIN_WAGER_LAMPORTS = 10_000 (0.00001 SOL) — ensures both fees ≥ 1 lamport
- MAX_WAGER_LAMPORTS = 100_000_000_000 (100 SOL) — prevents unfundable escrow
- **Invariant:** For any wager in [MIN, MAX], treasury_amount ≥ 1 AND ops_amount ≥ 1
- **Invariant:** For any wager in [MIN, MAX], total_pot fits u64 without overflow

### Lamport Transfer Conservation

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `settle_match` transfers (lines 284-291) | `programs/solshot-escrow/src/lib.rs` | Multi-account | 6 | no | no |
| `cancel_match` refunds (lines 358-367) | `programs/solshot-escrow/src/lib.rs` | Multi-account | 4 | no | no |
| `permissionless_reclaim` refunds (lines 420-429) | `programs/solshot-escrow/src/lib.rs` | Multi-account | 4 | no | no |

**Details:**
- `settle_match`: escrow -= winner_amount, then -= treasury_amount, then -= ops_amount
- **Invariant:** After all 3 transfers, escrow balance == rent_exempt_minimum (reclaimed by close)
- `cancel_match` / `permissionless_reclaim`: escrow -= wager for each deposited player
- **Invariant:** After refunds, escrow balance == original_lamports - (N_deposited * wager)
- **Risk:** If escrow doesn't have enough lamports (impossible with correct deposits, but needs proof)

### Account Space Sizing

| Function | File | Complexity | Signal Hits | Has GL Spec | SOS Flagged |
|----------|------|-----------|-------------|-------------|-------------|
| `GlobalConfig::SPACE` (line 706) | `programs/solshot-escrow/src/lib.rs` | Simple | 1 | no | no |
| `MatchEscrow::SPACE` (line 753) | `programs/solshot-escrow/src/lib.rs` | Simple | 1 | no | no |

**Details:**
- GlobalConfig: 8 + 32 + 32 + 32 + 1 + 1 = 106
- MatchEscrow: 8 + (4+32) + 32 + 32 + 32 + 8 + 1 + 1 + 1 + 8 + 8 + 1 = 168
- **Invariant:** SPACE values match actual serialized account size (prevents truncation/corruption)

### Constant Derivations

| Constant | Value | File | Notes |
|----------|-------|------|-------|
| `TREASURY_BPS` | 700 | `lib.rs:15` | 7% of pot |
| `OPS_BPS` | 300 | `lib.rs:16` | 3% of pot |
| `BPS_DENOMINATOR` | 10000 | `lib.rs:17` | Standard basis points |
| `TIMEOUT_SECONDS` | 86400 | `lib.rs:20` | 24 hours |
| `PERMISSIONLESS_RECLAIM_TIMEOUT` | 172800 | `lib.rs:23` | 48 hours (2 * TIMEOUT_SECONDS) |
| `SETTLEMENT_TIMEOUT_SECONDS` | 3600 | `lib.rs:26` | 1 hour |
| `MIN_WAGER_LAMPORTS` | 10_000 | `lib.rs:29` | 0.00001 SOL |
| `MAX_WAGER_LAMPORTS` | 100_000_000_000 | `lib.rs:32` | 100 SOL |

- **Invariant:** TREASURY_BPS + OPS_BPS < BPS_DENOMINATOR (fees < 100%)
- **Invariant:** PERMISSIONLESS_RECLAIM_TIMEOUT == 2 * TIMEOUT_SECONDS

---

## Uncategorized

No uncategorized math regions found. All signal hits mapped to known categories.

---

## Priority Ranking for Verification

| Priority | Region | Why |
|----------|--------|-----|
| 🔴 P0 | Fee Calculation (settle_match) | Highest economic impact — incorrect BPS math = lost funds |
| 🔴 P0 | Lamport Transfer Conservation | Must prove escrow is fully drained — stuck lamports = permanent loss |
| 🟡 P1 | Wager Bounds → Fee Guarantee | Must prove MIN_WAGER ensures fees ≥ 1 lamport |
| 🟡 P1 | Pot Calculation Overflow | Must prove wager * 2 fits u64 at MAX_WAGER |
| 🟢 P2 | Timeout Arithmetic | Low risk (i64 timestamp + seconds), but easy to prove |
| 🟢 P2 | Account Space Sizing | Should match Anchor serialization — low risk, quick check |
