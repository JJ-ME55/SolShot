## H079 — IDL Sync Drift

**Verdict:** NOT VULNERABLE

**Severity:** INFO

**Evidence:**
Binary comparison confirms `target/idl/solshot_escrow.json` (23949 bytes, Feb 23 18:38) and `server/idl/solshot_escrow.json` (23949 bytes, Feb 22 22:03) are byte-identical. Files match exactly via `cmp` utility.

IDL is manually copied from Anchor build output to server directory post-build. No automatic CI check enforces consistency, but manual verification shows current state is in sync.

**Observation:** No automated build step copies IDL on Anchor recompile. Risk: Anchor program update without IDL copy → RPC calls via stale interface definition → potential deserialization errors.

**Recommendation:** Add post-build script to CI/CD that verifies IDL hash matches or copies automatically. Document manual sync process as temporary measure.
