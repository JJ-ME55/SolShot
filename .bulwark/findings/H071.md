## H071 — BN Import Path Fragility: Direct bn.js Import

**Verdict:** POTENTIAL

**Severity:** LOW

**Evidence:**
- `server/services/escrow.js:25` — `import BN from 'bn.js'`
- Direct import from npm `bn.js` (not from `@coral-xyz/anchor`)
- `package.json` lists `bn.js` as direct dependency (version not explicitly pinned in query)
- Anchor 0.32.1 uses `bn.js` internally, but importing directly creates version mismatch risk

**Recommendation:** Verify `bn.js` version in package.json; consider importing from Anchor's re-export if available, or pin bn.js to exact version.
