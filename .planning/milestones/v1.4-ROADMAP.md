# Milestone v1.4: N-Player Escrow

**Status:** SHIPPED 2026-02-28
**Phases:** 20-23
**Total Plans:** 10

## Overview

Upgrade the Anchor escrow program and full stack to support 2-4 player wagered matches with trustless winner-takes-all settlement and partial deposit handling.

## Phases

### Phase 20: Anchor Program

**Goal:** The MatchEscrow on-chain program supports 2-4 players: deposits tracked via bitmap, settlement calculates pot from all depositors, partial deposits can start or cancel, and the 10-minute timeout applies.
**Depends on:** Phase 19 (complete)
**Plans:** 3 plans

Plans:
- [x] 20-01: Struct rewrite — MatchEscrow account (ESC-01, ESC-02, ESC-12, ESC-13, ESC-14)
- [x] 20-02: Core instructions — create_match, deposit_wager, settle_match (ESC-03, ESC-04, ESC-05, ESC-06, ESC-07, ESC-10)
- [x] 20-03: Refund instructions — cancel_match, permissionless_reclaim, start_with_depositors (ESC-08, ESC-09, ESC-11)

**Details:**
- MatchEscrow struct rewritten: `players: [Pubkey; 4]` + `max_players: u8` + `deposits_mask: u8` bitmap
- SPACE 168→232, TIMEOUT_SECONDS 86400→600 (24h→10min)
- Settlement: `wager * deposits_mask.count_ones()` with 90/7/3 BPS split
- Cancel/reclaim use `remaining_accounts` pattern — no named player accounts
- `start_with_depositors` compacts players[] array before reducing max_players
- All 69 cargo tests pass, IDL synced to server/idl/

### Phase 21: Server Escrow Services

**Goal:** The server-side service layer (escrow.js and solana.js) can create, settle, and cancel N-player escrow accounts, with correct pot math and N-player remaining_accounts for cancel.
**Depends on:** Phase 20
**Plans:** 2 plans

Plans:
- [x] 21-01: escrow.js N-player update — createMatchEscrow, settleMatchEscrow, cancelMatchEscrow, getEscrowState (SRV-01, SRV-03, SRV-04, SRV-05, SRV-17)
- [x] 21-02: solana.js integration — settleMatch, refundWager, calculateSettlement (SRV-02, SRV-06, SRV-07, SRV-08)

**Details:**
- `createMatchEscrow` accepts `playerAddresses[]` array (2-4)
- `cancelMatchEscrow` uses `.remainingAccounts()` builder pattern
- `getEscrowState` returns N-player fields + backward-compat shims
- `settleMatch` takes `playerCount` param, `totalPot = wagerSOL * playerCount`
- `refundWager` accepts `playerAddresses[]` array
- All main.js call sites converted to array form with `.filter(Boolean)`

### Phase 22: Server Socket Handlers

**Goal:** The game server orchestrates N-player escrow from room-full through match start, handles partial deposit timeout with host choice, and records SHOT milestones + playAgain state correctly for all N players.
**Depends on:** Phase 21
**Plans:** 3 plans

Plans:
- [x] 22-01: N-player escrow orchestration — deposit timeout, wallet collection, parallel TX dispatch, depositsMask, escrowDepositStatus (SRV-09, SRV-10, SRV-11, SRV-12, SRV-18)
- [x] 22-02: Partial deposit flow — 3-branch timeout, escrowPartialDeposit, escrowPartialStart, escrowCancelAll, wager guard removal (SRV-13, SRV-14, SRV-15, SRV-16)
- [x] 22-03: Tech debt — N-player SHOT milestones, prestige, DB persist, playAgain maxPlayers, failedSettlements (DEBT-01, DEBT-02)

**Details:**
- DEPOSIT_TIMEOUT_MS changed from 120s to 300s (5 minutes)
- 3-branch deposit timeout: all-deposited (noop), zero (cancel+destroy), partial (host decision)
- `escrowPartialStart` kicks non-depositors, compacts room, calls start_with_depositors on-chain
- `escrowCancelAll` refunds depositors, preserves room
- Wager guard removed — 3-4 player wagered matches allowed
- SHOT milestone recording loops over all N players
- `resetForPlayAgain` passes maxPlayers to createMatchState
- `failedSettlements` uses allWallets[] shape

### Phase 23: Client UX

**Goal:** Players in any wagered room see real-time deposit status for all participants, a countdown timer, and the host can make the partial deposit choice from the lobby; the battle HUD shows the live pot, and all match modes are selectable for 3-4 player rooms.
**Depends on:** Phase 22
**Plans:** 2 plans

Plans:
- [x] 23-01: LobbyScreen deposit flow — per-player status badges, countdown timer, partial deposit host UI, kick notification, formatWagerWithPayout N-player fix (CLT-01, CLT-02, CLT-03, CLT-04, CLT-05, CLT-07, CLT-08)
- [x] 23-02: BattleScreen potDisplay N-player fix (CLT-06)

**Details:**
- Per-player DEPOSITED/PENDING badges driven by escrowDepositStatus events
- Countdown timer from server depositDeadlineMs with urgency styling at 30s, pulse at 10s
- Host sees START WITH N / CANCEL AND REFUND buttons on partial timeout
- Non-host sees WAITING FOR HOST DECISION
- Kicked player sees modal with reason and returns to menu
- `formatWagerWithPayout(amount, players=2)` calculates N-player pot
- BattleScreen potDisplay: `wager * numPlayersInMatch` with fallback chain
- All 5 match modes available for 3-4 player rooms (no player count filter)

---

## Milestone Summary

**Key Decisions:**
- Winner-takes-all N-player (not placement split) — same model as 2-player, simpler
- Equal wagers only — simpler PDA, fairer gameplay
- 5-10 min deposit timeout (not 24h) — more players = higher no-show risk
- Partial deposit: host chooses start or cancel — depositors decide, not auto-cancel
- pot = wager * deposits_mask.count_ones() — actual depositors, not registered count
- remaining_accounts pattern for cancel/reclaim — no named player accounts in structs
- start_with_depositors compacts players[] array — contiguous depositor slots after partial-start

**Issues Resolved:**
- settleMatch called without playerCount at 3 call sites (GAP-1, fixed in aa4fc69)
- Disconnect even-score refund only passed 2 players (GAP-2, fixed in aa4fc69)
- Lobby-state disconnect refund sliced first 2 wallets only (GAP-3, fixed in aa4fc69)

**Issues Deferred:**
- tests/solshot-escrow.ts uses old 2-player API — TS integration tests cannot run against new program
- Stale comment on PERMISSIONLESS_RECLAIM_TIMEOUT says '172800 seconds' (actual: 1200)
- Stale comment 'Both players deposited' at line ~2197

**Technical Debt Incurred:**
- TS test suite outdated (69 Rust cargo tests cover all functionality)
- Two stale comments in server code

**Stats:**
- 4 phases, 10 plans
- 41 commits
- 11 code files changed, +3,635 / -471 lines
- 2 days (27-28 Feb 2026)
- Git range: fdac49e → 33c02be
- Audit: 42/42 requirements, 18/18 integration, 5/5 flows — PASSED

---

_For current project status, see .planning/ROADMAP.md_
_Archived: 2026-02-28_
