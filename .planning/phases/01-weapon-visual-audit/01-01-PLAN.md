---
phase: 01-weapon-visual-audit
plan: 01-01
title: Weapon Visual Audit — Formal Confirmation Report
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md
autonomous: true

must_haves:
  truths:
    - "All 20 active weapon classes are confirmed aligned with server physics parameters"
    - "The 4 diff differences are documented with justification for each"
    - "WVA-02 is formally closed with a finding of no required fixes"
    - "Dead weapon classes are documented as out-of-scope"
  artifacts:
    - path: ".planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md"
      provides: "Formal audit report with weapon-by-weapon confirmation table"
      contains: "AUDIT FINDING: NO VISUAL DRIFT"
  key_links:
    - from: "AUDIT-REPORT.md"
      to: "server/services/physics.js WEAPON_DATA"
      via: "blast radius + damage factor cross-reference table"
      pattern: "blastRadius.*damageFactor"
    - from: "AUDIT-REPORT.md"
      to: "client/src/weapons/packs/Standard/Standard.js"
      via: "per-weapon class confirmation entries"
      pattern: "Confirmed|Intentional"
---

<objective>
Produce a formal audit report that confirms all 20 active weapon client classes in Standard.js are
correctly aligned with server physics parameters, and that the 4 differences found in the automated
diff against converted-repo.txt are each intentional and properly justified.

Purpose: Closes WVA-01 (audit) and WVA-02 (fix drift) with documented evidence. Establishes a
permanent record so future contributors do not mistakenly revert the heatseeker rewrite or the
prestige weapon rebalance values.

Output: AUDIT-REPORT.md at `.planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md`
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-weapon-visual-audit/01-RESEARCH.md
@client/src/weapons/packs/Standard/Standard.js
@server/services/physics.js
@client/src/weapons/array.js
@client/src/data/weapons.js
@client/src/data/tiers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cross-reference all 20 active weapons against server WEAPON_DATA</name>
  <files>
    .planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md
  </files>
  <action>
    Read these files in full before writing anything:
    - `client/src/weapons/array.js` — get the authoritative list of 20 active weapon IDs
    - `server/services/physics.js` WEAPON_DATA section (top of file, IDs 0-29) — get blastRadius and damageFactor for each active weapon
    - `client/src/weapons/packs/Standard/Standard.js` — for each active weapon class, locate the `blast()` method and read the `weapon.terrain.blast(...)` call (3rd positional arg before `- weapon.scene.tank1.hitRadius`) and the `weapon.defaultUpdateScore(...)` call (3rd and 4th positional args)

    For weapons that use non-standard blast patterns (Spider uses line segments, Hail Storm rains balls, Napalm uses particle spread, Ground Hog tunnels, Pile Driver drills), note the pattern type but still confirm the radius value.

    Build a complete cross-reference table: for each of the 20 active weapons, record:
    - Weapon ID, class name, display name
    - Server WEAPON_DATA blastRadius
    - Client Standard.js blast call radius (before hitRadius subtraction)
    - Server WEAPON_DATA damageFactor
    - Client defaultUpdateScore factor
    - Match status: MATCH / INTENTIONAL-DIFF / MISMATCH

    The research (01-RESEARCH.md) already found zero mismatches — this task confirms that finding by
    reading the actual source files. Do not skip the source file reads; the report must cite actual
    line numbers.

    Verification pitfall: Blast radius in WEAPON_DATA is the nominal value. In Standard.js the call
    reads `blastRadius - weapon.scene.tank1.hitRadius`. Compare the blastRadius value BEFORE that
    subtraction to WEAPON_DATA.blastRadius.

    For multi-hit weapons (Jackhammer, Pile Driver, Hail Storm, Spider): each hit may call blast
    separately. Confirm the radius used per-hit matches WEAPON_DATA, noting if subDamageFactor is
    used for sub-particles.
  </action>
  <verify>
    After reading source files, the cross-reference table must have exactly 20 rows (matching
    array.js instantiation count). Every row must have a status of MATCH or INTENTIONAL-DIFF —
    no MISMATCH rows. If any MISMATCH is found, note it but do not fix it in this plan; report it.
  </verify>
  <done>
    All 20 active weapon rows are populated with source-verified values and status. Zero MISMATCH
    rows confirmed (or any mismatches explicitly flagged for follow-up).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write AUDIT-REPORT.md with formal findings</name>
  <files>
    .planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md
  </files>
  <action>
    Write the audit report to `.planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md`.

    The report must contain all of the following sections:

    ## Header
    - Title: "Phase 1: Weapon Visual Audit — Formal Report"
    - Date: 2026-02-19
    - Auditor: Claude (automated cross-reference, source file reads)
    - Source files audited: Standard.js (current line count), physics.js, array.js, weapons.js, tiers.js

    ## Executive Summary
    - One paragraph stating the finding: all 20 active weapons are client-server aligned. WVA-01
      complete, WVA-02 finds no drift requiring correction.

    ## WVA-01: Audit Findings

    ### Active Weapon Cross-Reference Table
    Full table from Task 1. Columns: ID | Class | Name | Server blastRadius | Client radius | Server damageFactor | Client factor | Status

    ### Intentional Differences (4 items, no fixes needed)

    For each of the 4 differences found in the automated diff:

    **Difference 1 — Heatseeker homing algorithm (ID 5)**
    - What changed: detection radius 200→250px, turn rate diff/10→diff*0.15, angle calculation clarified (was `Angle.Between(tank,proj)+PI`, now explicit `Angle.Between(proj,tank)`), one-shot `_homing` guard added, sprite rotation added
    - Why intentional: Git commit 2e86aab "Fix heatseeker homing" deliberately improved the algorithm. Server physics handles trajectory; this is rendering-only improvement.
    - Verdict: DO NOT REVERT. Current version is correct.

    **Difference 2 — Pineapple sub-particle factor (ID 22)**
    - Reference value: 30/20. Current value: 32/20. Server WEAPON_DATA: 32/20.
    - Why intentional: Weapon rebalance updated server damageFactor. Client was updated to match server. Reference is the old pre-rebalance value.
    - Verdict: Current Standard.js matches server. No fix needed.

    **Difference 3 — Homing Missile blast radius + factor (ID 24)**
    - Reference: blastRadius=60, damageFactor=20/60. Current: blastRadius=80, damageFactor=60/80. Server WEAPON_DATA: blastRadius=80, damageFactor=60/80.
    - Why intentional: Weapon rebalance significantly buffed Homing Missile. Client matches server.
    - Verdict: Current Standard.js matches server. No fix needed.

    **Difference 4 — Cruiser damage factor (ID 29)**
    - Reference: damageFactor=60/80. Current: damageFactor=80/80. Server WEAPON_DATA: damageFactor=80/80.
    - Why intentional: Weapon rebalance buffed Cruiser to full damage. Client matches server.
    - Verdict: Current Standard.js matches server. No fix needed.

    ## WVA-02: Visual Drift Remediation
    State clearly: "FINDING: No visual drift requiring remediation was identified. All 20 active weapon
    classes produce blast effects consistent with server physics parameters. WVA-02 is closed with no
    action taken."

    ## Dead Weapon Classes (Out of Scope)
    Table of the 10 dead classes (IDs 3, 6, 8, 13, 14, 18, 19, 23, 27, 28) with note that they are
    never instantiated in array.js and excluded from audit scope per Litepaper v2.0 removal decisions.

    ## Common Pitfalls for Future Maintainers
    Copy the 4 pitfalls from 01-RESEARCH.md verbatim. These are essential institutional knowledge.

    ## Open Items for WVA-03
    List the 3 open questions from the research that are relevant to manual testing:
    1. Prestige weapons need in-game playtesting since the rebalance
    2. Heatseeker sprite rotation visual confirmation needed
    3. Napalm particle scoring reconciliation (out of scope for visual audit, noted for awareness)
  </action>
  <verify>
    The file `.planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md` exists and contains all
    required sections. Verify by checking file size is >100 lines and the key phrase
    "WVA-02 is closed with no action taken" appears in the file.
  </verify>
  <done>
    AUDIT-REPORT.md is written with all sections populated from actual source file reads. The report
    can stand alone as evidence for a code review that no visual drift exists.
  </done>
</task>

</tasks>

<verification>
1. `AUDIT-REPORT.md` exists at `.planning/phases/01-weapon-visual-audit/AUDIT-REPORT.md`
2. The cross-reference table contains exactly 20 rows (15 base + 5 prestige)
3. No MISMATCH status rows in the table
4. All 4 intentional differences are documented with justification
5. WVA-02 closure statement is explicit and clear
6. Dead weapon section lists all 10 removed weapons
7. Pitfalls section preserved for future maintainers
</verification>

<success_criteria>
- AUDIT-REPORT.md is written and complete
- Any maintainer reading the report understands why converted-repo.txt differences are NOT bugs
- WVA-01 and WVA-02 are formally closed with written evidence
- Future contributors are warned against reverting heatseeker or prestige weapon values
</success_criteria>

<output>
After completion, create `.planning/phases/01-weapon-visual-audit/01-01-SUMMARY.md` following the
summary template at `@C:\Users\johnk\.claude/get-shit-done/templates/summary.md`.

The summary should note:
- What was produced: AUDIT-REPORT.md
- Key finding: zero visual drift, 4 intentional differences documented
- WVA-01 and WVA-02 status: CLOSED
- Handoff to 01-02: checklist can reference the cross-reference table for blast color/effect details
</output>
