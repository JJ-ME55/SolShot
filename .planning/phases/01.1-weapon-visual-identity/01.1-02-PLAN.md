---
phase: 01.1-weapon-visual-identity
plan: 01.1-02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - client/src/weapons/packs/Standard/Standard.js
autonomous: true

must_haves:
  truths:
    - "Big Shot fires a noticeably larger (5-6px) deep magenta projectile with a thick smoky trail"
    - "Skipper produces a visible dust/spark puff at each bounce point on the terrain"
    - "Hailstorm sub-projectiles are ice-blue angular shards (not circles), with frost-puff on terrain contact"
    - "Crazy Ivan projectile visibly wobbles (1-2px jitter) and shifts color purple-to-red during flight, with erratic stuttery trail"
    - "Tommy Gun fires bright tracer rounds with alternating blue-white hot streaks and muzzle flash pulses"
  artifacts:
    - path: "client/src/weapons/packs/Standard/Standard.js"
      provides: "Enhanced Tier 2 weapon visuals (bigshot, skipper, hailstorm, crazyivan, tommygun)"
      contains: "jitter"
  key_links:
    - from: "client/src/weapons/packs/Standard/Standard.js"
      to: "client/src/classes/Weapon.js"
      via: "weapon.spawnParticle() and weapon.spawnBurstEffect() calls for bounce puffs and effects"
      pattern: "spawnParticle|spawnBurstEffect"
---

<objective>
Enhance all 5 Tier 2 weapons (Big Shot, Skipper, Hailstorm, Crazy Ivan, Tommy Gun) with visually distinct projectile shapes, trail effects, and flight animations per the design doc.

Purpose: These weapons get "distinct personality" -- each should be instantly recognizable by projectile appearance and trail character during flight.
Output: 5 Tier 2 weapons with unique visual identity in Standard.js.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Docs/plans/2026-02-19-weapon-visual-identity-design.md
@.planning/phases/01.1-weapon-visual-identity/01.1-01-SUMMARY.md
@client/src/classes/Weapon.js
@client/src/weapons/packs/Standard/Standard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Big Shot and Skipper</name>
  <files>client/src/weapons/packs/Standard/Standard.js</files>
  <action>
Modify 2 weapon classes in Standard.js. For each weapon, modify ONLY the specific methods listed.

**A) Big Shot (class bigshot, line ~127, ID:1)**
- `create()`: Change arc radius from 2 to 5 (much larger projectile). Change fill color to `rgba(180,0,150,1)` (deep magenta, darker/heavier than the existing bright magenta). The canvas is already 20x40 which accommodates 5px radius fine.
- `update()`: Change updateTail params to: factor 10 (slower fade = longer trail), minimum 8 (longer minimum trail), thickness 8 (THICK smoky trail), color `{r: 180, g: 0, b: 150}` (deep magenta matching projectile). Keep taper `true`. This creates a heavy, thick smoke plume behind a large projectile.
- NO changes to blast(), hit methods, or any other methods. Do NOT change blast radius values.

**B) Skipper (class skipper, line ~3620, ID:20)**
- The projectile itself is already fine -- 2px cyan circle with good bouncing behavior.
- `skipperBounce()` (line ~3720): After the existing `weapon.scene.sound.play('skipperbounce')` and before `return weapon.defaultBounce(obj)`, add a dust/spark puff at the current bounce point using `weapon.spawnBurstEffect()`:
  ```
  weapon.spawnBurstEffect(
      obj.body.x, obj.body.y,
      6,          // count: 6 particles
      0xCCBB88,   // sandy/dust color
      12,         // spread: 12px
      0.8,        // size
      300         // lifetime: 300ms
  )
  ```
  Also add the same burst effect in `onTerrainHit()` just before `this.skipperBounce(weapon, obj)` is called (inside the `else if (this.bounce > 0)` block). This way every terrain bounce produces a visible puff.
- Reduce the puff count by 1 for each successive bounce to make them slightly smaller over time. Track bounce count: the class already has `this.bounce` which starts at 4 and decrements. Use it: `count = Math.max(3, this.bounce + 2)` for puff count.
- NO changes to create(), update(), blast(), or any other methods.

CRITICAL: Do NOT modify any weapon class other than bigshot and skipper. Do NOT change any blast radius, damage factor, or score calculation values. Heatseeker (class heatseeker) must NOT be touched.
  </action>
  <verify>
Run: `cd C:/Users/johnk/SolShot && npx react-scripts build 2>&1 | tail -5` -- must compile without errors. Verify: `grep -n "spawnBurstEffect" client/src/weapons/packs/Standard/Standard.js` should show hits in skipper class. `grep -n "radius.*5" client/src/weapons/packs/Standard/Standard.js | head -5` should show Big Shot's larger radius.
  </verify>
  <done>Big Shot fires a 5px deep magenta projectile with thick smoky trail. Skipper produces dust puffs at each bounce point. No gameplay/damage changes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Hailstorm, Crazy Ivan, and Tommy Gun</name>
  <files>client/src/weapons/packs/Standard/Standard.js</files>
  <action>
Modify 3 weapon classes in Standard.js. For each weapon, modify ONLY the specific methods listed.

**A) Hailstorm (class hailstorm, line ~2860, ID:16)**
- `create()`: Change main projectile from 2px circle to 4px. Change fill color to `rgba(180,220,255,1)` (ice blue-white).
- `update()`: Change updateTail color to `{r: 180, g: 220, b: 255}` (matching ice-blue).
- `spawnBall()` (search for `spawnBall` method in hailstorm class): The sub-projectiles are currently small green circles. Change them to ice-blue angular shapes. In the `spawnBall` method's canvas drawing, replace the circle drawing with an angular ice shard shape:
  ```
  // Ice shard: angular polygon instead of circle
  ctx.fillStyle = 'rgba(180,230,255,1)'
  ctx.beginPath()
  ctx.moveTo(canvas.width/2, canvas.height/2 - 3)
  ctx.lineTo(canvas.width/2 + 2, canvas.height/2)
  ctx.lineTo(canvas.width/2 + 1, canvas.height/2 + 3)
  ctx.lineTo(canvas.width/2 - 1, canvas.height/2 + 2)
  ctx.lineTo(canvas.width/2 - 2, canvas.height/2 - 1)
  ctx.closePath()
  ctx.fill()
  ```
  If the sub-projectile color was green (like `rgba(0,230,80,1)` or similar), change it to ice-blue. Search for the actual fill color used in spawnBall and replace.
- `onTerrainHit()`: When a sub-projectile (not the main projectile) hits terrain, add a frost puff: inside the sub-projectile branch (where `obj !== this.projectile` or equivalent), before the bounce/destroy logic, call `weapon.spawnBurstEffect(obj.body.x, obj.body.y, 4, 0xDDEEFF, 8, 0.6, 250)` -- small icy white burst.
- NO changes to scoring, blast(), or dissociate logic beyond visual color.

**B) Crazy Ivan (class crazyivan, line ~1346, ID:9)**
- `create()`: Change fill color to `rgba(180,50,200,1)` (purple, starting color for the shift).
- `update()`: In the `this.dissipated === false` branch, BEFORE updateTail, add wobble jitter: offset the projectile position by 1-2px random noise each frame. Use: `this.projectile.body.x += (Math.random() - 0.5) * 2` and same for y. This creates visible instability. Also add color shift: use a frame counter `this._colorFrame` (add to constructor/reset, default 0, increment each frame). Compute shifting color: `var r = 180 + Math.sin(this._colorFrame * 0.15) * 60` and `var b = 200 - Math.sin(this._colorFrame * 0.15) * 80`. Pass to updateTail as `{r: Math.floor(r), g: 50, b: Math.floor(b)}`. This creates a purple-to-red color oscillation. Change updateTail taper to `false` to make the trail more erratic/ragged.
- NO changes to dissipated phase, sub-projectile behavior, checkCloseToTank, blast(), or scoring.

**C) Tommy Gun (class tommygun, line ~4743, ID:26)**
- `makeTexture()`: Change bullet appearance from 1px circle to a 2px-wide, 4px-long bright tracer streak. Replace the arc drawing with:
  ```
  // Alternating blue-white tracers with gold tint for prestige
  var isBlue = index % 2 === 0
  ctx.fillStyle = isBlue ? 'rgba(150,180,255,1)' : 'rgba(255,240,200,1)'
  ctx.fillRect(canvas.width/2 - 2, canvas.height/2 - 0.5, 4, 1)
  ```
  The `index` parameter is already passed to makeTexture. This gives alternating blue/gold-white tracer rounds.
- `shoot()`: Inside the setInterval callback, after `weapon.scene.sound.play('rungun')`, add a muzzle flash: `weapon.spawnBurstEffect(weapon.turret.x + (weapon.turret.height/2) * Math.sin(weapon.turret.rotation), weapon.turret.y - (weapon.turret.height/2) * Math.cos(weapon.turret.rotation), 3, 0xFFEECC, 6, 0.5, 100)` -- small warm-white flash per bullet.
- `update()`: Change updateTail color to alternate per bullet. Use: `var c = obj.index % 2 === 0 ? {r: 150, g: 180, b: 255} : {r: 255, g: 240, b: 200}`. Pass `c` as color to updateTail. Change thickness to 1 (thinner tracer trails).
- NO changes to blast(), scoring, or hit methods.

CRITICAL: Do NOT modify any weapon class other than hailstorm, crazyivan, and tommygun. Do NOT change any blast radius, damage factor, or score calculation values. Do NOT modify heatseeker or any Tier 1 weapons already done in Plan 01.
  </action>
  <verify>
Run: `cd C:/Users/johnk/SolShot && npx react-scripts build 2>&1 | tail -5` -- must compile without errors. Verify changes: `grep -n "_colorFrame\|ice shard\|isBlue" client/src/weapons/packs/Standard/Standard.js` should show hits for Crazy Ivan color shift, Hailstorm ice shards, and Tommy Gun alternating tracers.
  </verify>
  <done>Hailstorm has ice-blue projectile with angular ice shard sub-projectiles and frost puffs. Crazy Ivan wobbles and shifts purple-to-red with erratic trail. Tommy Gun fires alternating blue/gold-white tracer streaks with muzzle flash per bullet. No gameplay/damage changes.</done>
</task>

</tasks>

<verification>
1. `cd C:/Users/johnk/SolShot && npx react-scripts build` completes without errors
2. Big Shot projectile radius is 5px (not 2px), trail thickness is 8 (not 4)
3. Skipper skipperBounce and onTerrainHit call spawnBurstEffect
4. Hailstorm sub-projectiles draw angular shapes (not circles), main projectile is 4px ice-blue
5. Crazy Ivan has wobble jitter in update and color-shifting trail
6. Tommy Gun bullets are tracer streaks (not circles) with muzzle flash
7. No blast radius, damage factor, or scoring values changed in any weapon
8. Heatseeker class is completely untouched
</verification>

<success_criteria>
- Big Shot: 5px deep magenta projectile, thick 8px smoky trail
- Skipper: Dust puff burst at each terrain bounce point
- Hailstorm: 4px ice-blue main projectile, angular ice shard sub-projectiles, frost puffs
- Crazy Ivan: 1-2px wobble jitter, purple-to-red color shift, erratic trail
- Tommy Gun: Alternating blue/gold-white tracer streaks, muzzle flash per bullet
- Zero gameplay changes -- no blast/damage/score modifications
- Project builds successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-weapon-visual-identity/01.1-02-SUMMARY.md`
</output>
