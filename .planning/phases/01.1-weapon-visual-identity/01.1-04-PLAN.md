---
phase: 01.1-weapon-visual-identity
plan: 01.1-04
type: execute
wave: 3
depends_on: ["01.1-02", "01.1-03"]
files_modified:
  - client/src/weapons/packs/Standard/Standard.js
autonomous: false

must_haves:
  truths:
    - "Homing Missile fires a bulky missile shape with thick exhaust plume and visible stall-and-drop animation"
    - "Cruiser has rolling sparks and tread marks during terrain roll phase"
    - "Chain Reaction projectile is a 4px white-hot orb with heavy spark trail, and chain detonations have visible energy arcs between them"
    - "Pineapple fires a 6-8px green pulsing grenade with heavy green particle smoke, green flash on scatter, and 20 mini-trailed sub-munitions"
    - "All 19 enhanced weapons (excluding Heatseeker) are visually distinct from each other when fired in Practice mode"
    - "No two weapons have the same projectile size + shape + trail combination"
  artifacts:
    - path: "client/src/weapons/packs/Standard/Standard.js"
      provides: "Enhanced Tier 4 prestige weapon visuals (homingmissile, cruiser, chainreaction, pineapple) plus all 20 weapons visually verified"
      contains: "exhaust plume"
  key_links:
    - from: "client/src/weapons/packs/Standard/Standard.js"
      to: "client/src/classes/Weapon.js"
      via: "weapon.spawnParticle() calls for exhaust, rolling sparks, energy arcs, green smoke"
      pattern: "spawnParticle|spawnBurstEffect"
---

<objective>
Enhance all 4 Tier 4 prestige weapons (Homing Missile, Cruiser, Chain Reaction, Pineapple) with elite/fear-inducing visual treatment, then verify all 20 weapons through a visual QA play-test.

Purpose: Prestige weapons are the "oh shit" tier -- opponents should immediately recognize and fear these weapons. They need the most dramatic visual treatment. The QA pass confirms all 20 weapons have unique visual identity.
Output: 4 prestige weapons with elite visual treatment in Standard.js, plus human-verified visual QA of all 20 weapons.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Docs/plans/2026-02-19-weapon-visual-identity-design.md
@.planning/phases/01.1-weapon-visual-identity/01.1-01-SUMMARY.md
@.planning/phases/01.1-weapon-visual-identity/01.1-02-SUMMARY.md
@.planning/phases/01.1-weapon-visual-identity/01.1-03-SUMMARY.md
@client/src/classes/Weapon.js
@client/src/weapons/packs/Standard/Standard.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Homing Missile and Cruiser prestige weapons</name>
  <files>client/src/weapons/packs/Standard/Standard.js</files>
  <action>
Modify 2 weapon classes. For each, modify ONLY the specific methods listed.

**A) Homing Missile (class homingmissile, line ~4301, ID:24 -- Bronze Prestige)**

Currently uses the same missile sprite as Heatseeker but with a different homing mechanic (checkAboveTank: stall above, then drop straight down). Needs differentiation.

- `create()`: Replace the current sprite (identical to Heatseeker) with a bulkier missile shape. Wider body, larger fins, red-orange coloring:
  ```
  canvas.height = 30
  canvas.width = 30

  // Wider missile body (bulkier than Heatseeker)
  ctx.fillStyle = 'rgba(220,80,20,1)'
  ctx.beginPath()
  ctx.moveTo(3, 13)
  ctx.lineTo(16, 12)
  ctx.arc(canvas.width/2, canvas.height/2, 3, -Math.PI/2, Math.PI/2)
  ctx.lineTo(3, 17)
  ctx.closePath()
  ctx.fill()

  // Larger tail fins (red-orange)
  ctx.fillStyle = 'rgba(200,40,0,1)'
  ctx.beginPath()
  ctx.moveTo(3, 13)
  ctx.lineTo(11, 13)
  ctx.lineTo(3, 7)
  ctx.closePath()
  ctx.fill()

  ctx.beginPath()
  ctx.moveTo(3, 17)
  ctx.lineTo(11, 17)
  ctx.lineTo(3, 23)
  ctx.closePath()
  ctx.fill()
  ```
  This creates a wider, more menacing missile shape in red-orange (vs Heatseeker's white with small red fins).

- `update()`: Add thick exhaust plume particles during flight. Before `weapon.defaultUpdate(this.projectile)`:
  ```
  // Exhaust plume -- dense smoke particles trailing behind
  if (this.projectile.body !== undefined) {
      for (var e = 0; e < 3; e++) {
          weapon.spawnParticle(
              this.projectile.body.x + (Math.random() - 0.5) * 4,
              this.projectile.body.y + (Math.random() - 0.5) * 4,
              Math.random() > 0.5 ? 0xAAAAAA : 0x888888,
              1.2 + Math.random() * 0.8,
              800 + Math.random() * 400,
              { x: (Math.random() - 0.5) * 4, y: (Math.random() - 0.5) * 4 }
          )
      }
  }
  ```
  This creates a thick lingering smoke plume behind the missile. The longer lifetime (800-1200ms) means smoke hangs in the air.

- `checkAboveTank()`: After the velocity angle is set to `Math.PI/2` (the drop), add an intensified exhaust burst:
  ```
  // Stall-and-drop visual: burst of exhaust at stall point
  weapon.spawnBurstEffect(this.projectile.body.x, this.projectile.body.y, 12, 0xCCCCCC, 15, 1.5, 600)
  ```
  Place this AFTER the `this.canTurn = false` line and AFTER the sound.play call.

- NO changes to blast(), hit methods, or scoring.

**B) Cruiser (class cruiser, line ~5199, ID:29 -- Silver Prestige)**

- `create()`: Change fill color to a metallic silver-white with slight gradient effect. Replace the 2px circle:
  ```
  // Metallic silver projectile
  var g = ctx.createRadialGradient(canvas.width/2, canvas.height/2, 0, canvas.width/2, canvas.height/2, 4)
  g.addColorStop(0, 'rgba(255,255,255,1)')
  g.addColorStop(0.5, 'rgba(200,210,220,1)')
  g.addColorStop(1, 'rgba(150,160,170,1)')
  ctx.fillStyle = g
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 3.5, 0, Math.PI * 2)
  ctx.closePath()
  ctx.fill()
  ```
  Larger (3.5px) with metallic gradient.

- `update()`: In the flight phase (`this.rolling === false`), change updateTail to: factor 12, minimum 5, thickness 5 (slightly thicker clean trail), color `{r: 200, g: 210, b: 220}` (silver-white). In the rolling phase (`this.rolling === true`), the existing `roll()` method already spawns fading circles (line ~5393). Enhance these: in the `roll()` method, change the existing circle creation from `weapon.scene.add.circle(this.projectile.body.x, this.projectile.body.y, 2, 0xeeeeee, 0.2)` to use `0xDDDDDD` with alpha 0.4 for brighter visibility. Also add rolling spark particles: every 3rd call to roll(), spawn 2 spark particles:
  ```
  // Rolling sparks (every 3rd frame)
  if (this._rollFrame === undefined) this._rollFrame = 0
  this._rollFrame++
  if (this._rollFrame % 3 === 0) {
      weapon.spawnParticle(
          this.projectile.body.x, this.projectile.body.y,
          0xFFDD88, 0.8, 300,
          { x: (Math.random() - 0.5) * 6, y: -(2 + Math.random() * 4) }
      )
      weapon.spawnParticle(
          this.projectile.body.x, this.projectile.body.y,
          0xFFEEAA, 0.6, 200,
          { x: (Math.random() - 0.5) * 8, y: -(1 + Math.random() * 3) }
      )
  }
  ```
  Place this in the `roll()` method, after the existing circle/tween code (around line 5398).

- Add `this._rollFrame = 0` to the `reset()` method.
- NO changes to blast(), onTerrainHit() logic, makeRollingTexture(), or scoring.

CRITICAL: Do NOT modify Heatseeker. Do NOT modify any other weapon class. Do NOT change blast radius, damage factor, or scoring values.
  </action>
  <verify>
Run: `cd C:/Users/johnk/SolShot && npx react-scripts build 2>&1 | tail -5` -- must compile without errors. Verify: `grep -n "exhaust plume\|Metallic silver\|_rollFrame\|0xAAAAAA" client/src/weapons/packs/Standard/Standard.js` should show Homing Missile exhaust and Cruiser rolling sparks.
  </verify>
  <done>Homing Missile fires a bulky red-orange missile with thick exhaust plume and stall-burst on drop. Cruiser has metallic silver 3.5px projectile with rolling sparks during terrain roll phase. No gameplay/damage changes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Chain Reaction and Pineapple prestige weapons</name>
  <files>client/src/weapons/packs/Standard/Standard.js</files>
  <action>
Modify 2 weapon classes. For each, modify ONLY the specific methods listed.

**A) Chain Reaction (class chainreaction, line ~3756, ID:21 -- Platinum Prestige)**

- `create()`: Change the main projectile from 0.5px radius (nearly invisible) to a 4px white-hot orb with glow. Replace the first canvas drawing (the 0.5px circle on 10x10 canvas) with:
  ```
  canvas.height = 20
  canvas.width = 20

  // Outer glow
  ctx.fillStyle = 'rgba(255,255,200,0.3)'
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 6, 0, Math.PI * 2)
  ctx.fill()
  // White-hot core
  ctx.fillStyle = 'rgba(255,255,240,1)'
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 3, 0, Math.PI * 2)
  ctx.fill()
  ```
  The existing particle emitters (emitter1, emitter2) that follow the projectile already provide a spark trail -- they use 'chainparticle' texture. Increase emitter1 alpha start to 0.7 (from 0.4) and lifespan to 500 (from 350) for heavier trail. Increase emitter2 scale end to 3.5 (from 2.5) for bigger pulse.

- `blast()`: The existing chain blast creates sequential explosions at 15 offset positions with 200ms delay. Add visible energy arcs between detonation points: inside the `createBlast` function, after each `weapon.terrain.blast(...)` call, add a line of particles connecting current blast position to the previous one:
  ```
  // Energy arc from previous blast to current
  if (i > 1) {
      var prevOff = arr[i-2]  // previous blast offset
      var dx = offx - prevOff.x
      var dy = offy - prevOff.y
      for (var arc = 0; arc < 5; arc++) {
          var t = arc / 5
          weapon.spawnParticle(
              Math.floor(initX) + prevOff.x + dx * t,
              Math.floor(initY) + prevOff.y + dy * t,
              0xFFFFFF, 1, 400,
              { x: (Math.random() - 0.5) * 4, y: (Math.random() - 0.5) * 4 }
          )
      }
  }
  ```
  Also for the first blast (outside createBlast), no arc needed.

  Note: `weapon` needs to be accessible inside `createBlast`. It is -- `createBlast` is a closure that already accesses `weapon` for the terrain.blast call. The references `this.projectile.y` may need to be checked (the projectile is destroyed by then), but `initX`/`initY` are captured before destroy.

- NO changes to emitter cleanup, scoring, or hit methods.

**B) Pineapple (class pineapple, line ~3947, ID:22 -- Diamond Prestige)**

- `create()`: Replace the current gray gradient sphere with a green pulsing grenade. Larger canvas, green coloring:
  ```
  canvas.height = 24
  canvas.width = 24

  // Outer green glow
  ctx.fillStyle = 'rgba(0,200,50,0.25)'
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 8, 0, Math.PI * 2)
  ctx.fill()
  // Grenade body
  ctx.fillStyle = 'rgba(0,180,60,1)'
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 5, 0, Math.PI * 2)
  ctx.fill()
  // Segment lines for grenade look
  ctx.strokeStyle = 'rgba(0,140,40,0.6)'
  ctx.lineWidth = 0.5
  ctx.beginPath()
  ctx.moveTo(canvas.width/2, canvas.height/2 - 5)
  ctx.lineTo(canvas.width/2, canvas.height/2 + 5)
  ctx.stroke()
  ctx.beginPath()
  ctx.moveTo(canvas.width/2 - 5, canvas.height/2)
  ctx.lineTo(canvas.width/2 + 5, canvas.height/2)
  ctx.stroke()
  // Bright core
  ctx.fillStyle = 'rgba(100,255,100,0.5)'
  ctx.beginPath()
  ctx.arc(canvas.width/2, canvas.height/2, 2, 0, Math.PI * 2)
  ctx.fill()
  ```

- `update()`: In the `this.dissociated === false` branch, add green particle smoke trail and pulsing:
  ```
  // Pulsing green glow
  if (this._pulseFrame === undefined) this._pulseFrame = 0
  this._pulseFrame++
  var s = 1 + Math.sin(this._pulseFrame * 0.15) * 0.12
  this.projectile.setScale(s)

  // Heavy green particle smoke
  if (this._pulseFrame % 2 === 0) {
      for (var g = 0; g < 2; g++) {
          weapon.spawnParticle(
              this.projectile.body.x + (Math.random() - 0.5) * 6,
              this.projectile.body.y + (Math.random() - 0.5) * 6,
              Math.random() > 0.5 ? 0x00BB33 : 0x009922,
              1 + Math.random(),
              700 + Math.random() * 300,
              { x: (Math.random() - 0.5) * 4, y: (Math.random() - 0.5) * 4 }
          )
      }
  }
  ```
  Keep the existing `weapon.defaultUpdate(this.projectile)` and `this.checkCloseToTank(weapon)`.

- `checkCloseToTank()`: After the `weapon.scene.sound.play('split')` call and before `this.dissociate(weapon)`, add a dramatic green flash: `weapon.spawnBurstEffect(this.projectile.body.x, this.projectile.body.y, 20, 0x00FF44, 40, 2, 400)` -- large green flash on scatter.

- `dissociate()`: Change sub-munition color from `rgba(0,230,80,1)` to a brighter green `rgba(0,255,100,1)`. Change sub-munition radius from 1 to 1.5.

- `update()` dissociated branch: The existing sub-munition trail via `weapon.updateTail(p, 15, 5, 2, {r: 0, g: 230, b: 80})` is fine -- these 20 mini green trails are the visual payoff. Change color to `{r: 0, g: 255, b: 100}` to match brighter sub-munitions.

- Add `this._pulseFrame = 0` to the `reset()` method.
- NO changes to blast(), scoring, or hit handler logic.

CRITICAL: Do NOT modify any weapon class other than chainreaction and pineapple. Do NOT change any blast radius, damage factor, or scoring values.
  </action>
  <verify>
Run: `cd C:/Users/johnk/SolShot && npx react-scripts build 2>&1 | tail -5` -- must compile without errors. Verify: `grep -n "White-hot core\|energy arc\|Grenade body\|green flash" client/src/weapons/packs/Standard/Standard.js` should show Chain Reaction white-hot orb and energy arcs, Pineapple grenade body and green flash.
  </verify>
  <done>Chain Reaction fires a 4px white-hot orb with heavy spark trail and visible energy arcs between chain detonations. Pineapple fires a 6-8px green pulsing grenade with heavy green smoke trail, green flash on scatter, and 20 bright green mini-trailed sub-munitions. No gameplay/damage changes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 19 weapon visual enhancements (20 weapons minus Heatseeker which was already perfect). Every weapon now has a unique projectile appearance, trail character, and/or flight animation. Prestige weapons have elite visual treatment.</what-built>
  <how-to-verify>
1. Start the dev server: `cd C:/Users/johnk/SolShot && npm run dev` (or however you normally run it)
2. Open a Practice match in the browser
3. Fire each of these 20 weapons and verify visual distinctness:

**Tier 1 (clean and simple):**
- Single Shot: Bright 3px cyan sphere, short crisp trail
- Dirt Ball: Lumpy brown sphere, dust particles drifting down
- Magic Wall: Blue-white slab tumbling in flight, sparkle particles
- Sniper Rifle: Thin white streak, fast-fading tracer trail, muzzle flash on fire

**Tier 2 (distinct personality):**
- Big Shot: Large 5px deep magenta, thick smoky trail
- Heatseeker: NO CHANGES -- already perfect (rocket shape, homing, exhaust)
- Skipper: Same projectile, dust puffs at each bounce point
- Hailstorm: Ice-blue main projectile, angular ice shard sub-projectiles, frost puffs
- Crazy Ivan: Wobbling projectile, purple-to-red color shift, erratic trail
- Tommy Gun: Alternating blue/gold-white tracer streaks, muzzle flash per bullet

**Tier 3 (complex and spectacular):**
- 3 Shot: Split flash at launch, 3 colored trails diverging
- Spider: 4px pulsing orb, burst ring on proximity trigger
- Pile Driver: Heavy magenta, thick trail, shockwave rings on drill-down
- Jackhammer: Square (existing), spark puffs on each bounce
- Ground Hog: Dirt eruptions above tunnel path, debris burst on exit
- Napalm: Orange-yellow fireball, flickering lingering flame trail

**Tier 4 (prestige fear factor):**
- Homing Missile (Bronze): Bulky red-orange missile, thick exhaust plume, stall-burst
- Cruiser (Silver): Metallic silver, rolling sparks during terrain roll
- Chain Reaction (Platinum): White-hot orb, energy arcs between chain detonations
- Pineapple (Diamond): Green pulsing grenade, green smoke, green flash, 20 mini-trailed sub-munitions

4. Verify NO two weapons look the same during flight
5. Verify prestige weapons feel noticeably more dramatic than base weapons
6. Verify zero gameplay changes -- damage values, blast radii, and scoring are unchanged
  </how-to-verify>
  <resume-signal>Type "approved" if all weapons look visually distinct and satisfying, or describe any issues that need fixing</resume-signal>
</task>

</tasks>

<verification>
1. `cd C:/Users/johnk/SolShot && npx react-scripts build` completes without errors
2. Homing Missile has bulky missile shape with exhaust plume and stall-burst
3. Cruiser has metallic gradient projectile and rolling spark particles
4. Chain Reaction has 4px white-hot orb and energy arc particles between chain blasts
5. Pineapple has green pulsing grenade with smoke trail and bright green sub-munitions
6. All 20 weapons are visually distinct when fired in Practice mode (human verified)
7. No blast radius, damage factor, or scoring values changed in any weapon
8. Heatseeker is completely untouched
9. Server physics.js is untouched
</verification>

<success_criteria>
- Homing Missile: Bulky red-orange missile, thick exhaust plume, stall-burst visual on drop
- Cruiser: 3.5px metallic silver projectile, rolling sparks + enhanced tread marks during terrain roll
- Chain Reaction: 4px white-hot glowing orb, heavy emitter trail, energy arc particles between detonations
- Pineapple: 6-8px green pulsing grenade with segments, heavy green smoke, green flash on scatter, 20 bright sub-munition trails
- Human verification: All 20 weapons fired in Practice mode, each visually distinct, prestige weapons feel elite
- Zero gameplay changes -- purely visual enhancement, server untouched
- Project builds and runs successfully
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-weapon-visual-identity/01.1-04-SUMMARY.md`
</output>
