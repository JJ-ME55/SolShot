---
phase: 02-todo-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/scenes/main/index.js
autonomous: false

must_haves:
  truths:
    - "All 7 missing sound effects (tracer, split, magicwall, zapper, skipperbounce, homing, sniper) play audibly when their associated weapons fire"
    - "No existing sound effects are broken or missing after the change"
  artifacts:
    - path: "client/src/scenes/main/index.js"
      provides: "7 new this.load.audio() calls in preload()"
      contains: "this.load.audio('tracer'"
    - path: "client/public/assets/sounds/others/tracer.wav"
      provides: "Tracer weapon sound effect"
    - path: "client/public/assets/sounds/others/split.wav"
      provides: "Split weapon sound effect"
    - path: "client/public/assets/sounds/others/magicwall.wav"
      provides: "Magic Wall weapon sound effect"
    - path: "client/public/assets/sounds/others/zapper.wav"
      provides: "Zapper weapon sound effect"
    - path: "client/public/assets/sounds/others/skipperbounce.wav"
      provides: "Skipper bounce sound effect"
    - path: "client/public/assets/sounds/others/homing.wav"
      provides: "Homing Missile weapon sound effect"
    - path: "client/public/assets/sounds/others/sniper.wav"
      provides: "Sniper Rifle weapon sound effect"
  key_links:
    - from: "client/src/scenes/main/index.js"
      to: "client/public/assets/sounds/others/*.wav"
      via: "this.load.audio(key, [path]) in preload()"
      pattern: "this\\.load\\.audio\\('(tracer|split|magicwall|zapper|skipperbounce|homing|sniper)'"
    - from: "client/src/weapons/packs/Standard/Standard.js"
      to: "client/src/scenes/main/index.js"
      via: "this.scene.sound.play(key) calls existing sound keys"
      pattern: "sound\\.play\\('(tracer|split|magicwall|zapper|skipperbounce|homing|sniper)'"
---

<objective>
Add the 7 missing weapon sound effects to the game so every weapon has audible feedback when fired or activated.

Purpose: Currently 7 weapons reference sound keys that are never loaded — the safe sound wrapper silently swallows the failures. Once WAV files are sourced and preload lines are added, these weapons will have distinct audio during gameplay.

Output: Updated preload() with 7 new audio load lines, plus 7 WAV files on disk ready for the build.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-completion/02-RESEARCH.md

Key references:
- `client/src/scenes/main/index.js` lines 55-89 — existing preload() block with 29 loaded sounds
- `client/src/weapons/packs/Standard/Standard.js` — weapon classes that call these 7 sound keys
- `client/public/assets/sounds/others/` — directory containing existing WAV files (29 present)
- Safe sound wrapper at lines 97-106 of index.js — catches missing audio keys silently
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 7 missing sound preload lines to MainScene</name>
  <files>client/src/scenes/main/index.js</files>
  <action>
Add 7 `this.load.audio()` lines to the `preload()` method in `client/src/scenes/main/index.js`, immediately after the existing block of `this.load.audio()` calls (after line 88, the `rocket` line).

Add these exact lines:
```javascript
this.load.audio('tracer', ['assets/sounds/others/tracer.wav']);
this.load.audio('split', ['assets/sounds/others/split.wav']);
this.load.audio('magicwall', ['assets/sounds/others/magicwall.wav']);
this.load.audio('zapper', ['assets/sounds/others/zapper.wav']);
this.load.audio('skipperbounce', ['assets/sounds/others/skipperbounce.wav']);
this.load.audio('homing', ['assets/sounds/others/homing.wav']);
this.load.audio('sniper', ['assets/sounds/others/sniper.wav']);
```

All filenames MUST be lowercase (Linux deploy is case-sensitive).

Before adding, grep Standard.js to confirm each key is actually called:
- `tracer` — line ~1014
- `split` — lines ~1446, 1712, 4189, 5327
- `magicwall` — line ~2165
- `zapper` — line ~2489
- `skipperbounce` — line ~3831
- `homing` — lines ~3682, 4564
- `sniper` — via `soundEffect` data property at line ~1996

After adding, run `npx react-app-rewired build` from the `client/` directory to verify no build errors.
  </action>
  <verify>
1. Grep index.js for all 7 keys: `grep -c "load.audio.*tracer\|load.audio.*split\|load.audio.*magicwall\|load.audio.*zapper\|load.audio.*skipperbounce\|load.audio.*homing\|load.audio.*sniper" client/src/scenes/main/index.js` — should return 7
2. `cd client && npx react-app-rewired build` completes without errors
  </verify>
  <done>All 7 sound keys are registered in preload(). Build succeeds. The safe sound wrapper will still silently fail for any key whose WAV file is not yet on disk — this is expected until Task 2 is complete.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Source 7 WAV files and verify in-game</name>
  <action>
John must source 7 WAV files and save them to `client/public/assets/sounds/others/` with exact filenames:

| Filename | Sound Description | Suggested Freesound.org Search |
|---|---|---|
| `tracer.wav` | Bullet whiz / supersonic crack | "bullet whiz" or "tracer round" |
| `split.wav` | Crack/snap of splitting projectile | "splitting crack" or "snap break" |
| `magicwall.wav` | Heavy stone/earth thud | "stone impact" or "earth thud" |
| `zapper.wav` | Electric arc/zap | "electric zap" or "tesla arc" |
| `skipperbounce.wav` | Cartoon boing/spring bounce | "boing spring" or "cartoon bounce" |
| `homing.wav` | Rocket whoosh / missile tracking beep | "missile whoosh" or "rocket fly" |
| `sniper.wav` | High-velocity rifle crack | "sniper shot" or "rifle crack" |

Source: freesound.org (filter by CC0 license). Download as WAV format.

CRITICAL: Save all filenames in lowercase exactly as shown. Linux is case-sensitive.
  </action>
  <how-to-verify>
1. Run `ls client/public/assets/sounds/others/ | grep -E "tracer|split|magicwall|zapper|skipperbounce|homing|sniper"` — should show 7 files
2. Build: `cd client && npx react-app-rewired build`
3. Start a Practice match and fire each of these weapons: Tommy Gun (tracer), 3 Shot (split), Magic Wall (magicwall), Crazy Ivan (zapper), Skipper (skipperbounce), Homing Missile (homing), Sniper Rifle (sniper)
4. Confirm each produces audible sound on fire/impact
  </how-to-verify>
  <resume-signal>Type "sounds done" after all 7 WAV files are in place and verified, or describe which sounds are still missing.</resume-signal>
</task>

</tasks>

<verification>
1. `grep -c "load.audio" client/src/scenes/main/index.js` returns 36 (29 existing + 7 new)
2. All 7 WAV files exist: `ls client/public/assets/sounds/others/{tracer,split,magicwall,zapper,skipperbounce,homing,sniper}.wav`
3. `cd client && npx react-app-rewired build` succeeds
4. In Practice mode, firing Tommy Gun, 3 Shot, Magic Wall, Crazy Ivan, Skipper, Homing Missile, and Sniper Rifle all produce distinct sounds
</verification>

<success_criteria>
All 7 missing sound effects are loaded by Phaser preload() and play correctly when their associated weapons fire in a live match. No existing sounds are broken.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-completion/02-01-SUMMARY.md`
</output>
