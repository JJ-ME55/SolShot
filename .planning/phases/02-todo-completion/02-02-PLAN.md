---
phase: 02-todo-completion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 8 escrow tests pass against the deployed devnet program"
    - "Escrow lifecycle (create, deposit, settle, cancel) works end-to-end on devnet"
  artifacts:
    - path: "tests/solshot-escrow.ts"
      provides: "8 integration tests for escrow program"
      contains: "it("
  key_links:
    - from: "Anchor.toml"
      to: "devnet cluster"
      via: "cluster = devnet"
      pattern: 'cluster\s*=\s*"devnet"'
    - from: "tests/solshot-escrow.ts"
      to: "CqvRC6mSJe2CrBtENVfCEPkgRW3WwxLSL9C1hgXz7GtD"
      via: "program ID reference"
      pattern: "CqvRC6mSJe2CrBtENVfCEPkgRW3WwxLSL9C1hgXz7GtD"
---

<objective>
Run the existing escrow integration tests against the deployed devnet program and confirm all 8 tests pass — create_match, deposit_wager (x2), double-deposit rejection, settle_match (90/7/3 split), cancel_match, access control, and math verification.

Purpose: Validates the on-chain escrow program works correctly before mainnet deployment. The program is already deployed to devnet; the tests are already written. This plan confirms they actually pass.

Output: Test run output showing 8/8 passing tests, or a diagnostic report identifying failures to address.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-completion/02-RESEARCH.md

Key references:
- `tests/solshot-escrow.ts` — 8 test cases covering full escrow lifecycle
- `Anchor.toml` — cluster=devnet, program ID CqvRC6mSJe2CrBtENVfCEPkgRW3WwxLSL9C1hgXz7GtD
- Wallet: `~/.config/solana/solshot-dev.json` (HPyVPj2VH9yBirr7FMgAJeDH8xJgaMKy5UnwLkjSnovk)
- Windows gotcha: run `anchor test` from Git Bash, NOT PowerShell (~ path resolution)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify test prerequisites and run escrow tests</name>
  <files></files>
  <action>
Step 1 — Verify prerequisites:
1. Confirm `Anchor.toml` has `cluster = "devnet"` and program ID matches `CqvRC6mSJe2CrBtENVfCEPkgRW3WwxLSL9C1hgXz7GtD`
2. Confirm solshot-dev.json keypair exists at the path specified in Anchor.toml `wallet` field
3. Run `anchor --version` to confirm Anchor CLI is available (expect 0.32.x)
4. Run `anchor test --help` to check if `--skip-deploy` flag is available
5. Check devnet SOL balance of the dev wallet: `solana balance --url devnet --keypair ~/.config/solana/solshot-dev.json`

Step 2 — Run the tests:
```bash
# From project root C:/Users/johnk/SolShot in Git Bash
anchor test --skip-local-validator
```

If `--skip-deploy` is available and you want to avoid redeploying:
```bash
anchor test --skip-local-validator --skip-deploy
```

Step 3 — Capture full test output (stdout + stderr).

If the `before()` hook fails with airdrop errors (429 Too Many Requests):
- Note the error
- Check if playerOne/playerTwo keypairs need manual funding
- Report the specific failure for John to address (may need to pre-fund wallets or wait for rate limit reset)

If tests fail for non-airdrop reasons:
- Capture the failing test name, error message, and stack trace
- Do NOT attempt to modify the test code — just report the failure
  </action>
  <verify>
1. Anchor.toml has `cluster = "devnet"` — confirmed by grep
2. Dev wallet keypair file exists and has SOL balance
3. `anchor test` command completes (pass or fail, but no crash/hang)
  </verify>
  <done>Test output captured — either all 8 pass or specific failures are identified with diagnostic detail.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm escrow test results</name>
  <what-built>Escrow integration test run against devnet. The test suite covers: create_match, deposit_wager (both players), double-deposit rejection, settle_match with 90/7/3 BPS split verification, cancel_match with refund, access control checks, and integer math verification.</what-built>
  <how-to-verify>
Review the test output presented by Task 1:

**If all 8 tests passed:**
- Confirm the output shows `8 passing`
- No action needed — type "tests passed"

**If tests failed due to airdrop rate limiting:**
1. Pre-fund test wallets with devnet SOL from https://faucet.solana.com
2. Modify `tests/solshot-escrow.ts` `before()` to skip airdrop for already-funded wallets
3. Re-run: `anchor test --skip-local-validator`

**If tests failed for other reasons:**
- Review the error message and determine if it's a test bug, config issue, or real program bug
- Report back with the specific failure for debugging
  </how-to-verify>
  <resume-signal>Type "tests passed" if all 8 pass. If failures occurred, describe the failing tests and errors so we can plan remediation.</resume-signal>
</task>

</tasks>

<verification>
1. `anchor test --skip-local-validator` output shows `8 passing` with zero failures
2. Test output confirms: create_match works, both deposits succeed, settle distributes 90/7/3, cancel refunds, access control blocks unauthorized callers
</verification>

<success_criteria>
All 8 escrow integration tests pass against the deployed devnet program without errors. The full escrow lifecycle (create → deposit → settle/cancel) is validated on-chain.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-completion/02-02-SUMMARY.md`
</output>
