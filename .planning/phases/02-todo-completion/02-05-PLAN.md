---
phase: 02-todo-completion
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - render.yaml
autonomous: false

must_haves:
  truths:
    - "render.yaml CORS_ORIGINS includes https://solshot.gg and https://www.solshot.gg"
    - "DNS records are documented with exact values for John to set at registrar"
    - "solshot.gg resolves to the Vercel-hosted client over HTTPS"
  artifacts:
    - path: "render.yaml"
      provides: "Updated CORS_ORIGINS with solshot.gg domains"
      contains: "solshot.gg"
  key_links:
    - from: "render.yaml"
      to: "server CORS configuration"
      via: "CORS_ORIGINS env var"
      pattern: "CORS_ORIGINS.*solshot\\.gg"
---

<objective>
Update the server CORS configuration to allow requests from solshot.gg, and provide John with the exact DNS records needed to point solshot.gg to Vercel.

Purpose: The domain solshot.gg is registered but not yet pointed to the game client. The server also needs to accept requests from this origin. This plan handles the code-side CORS update and documents the DNS setup steps for John to execute at his domain registrar.

Output: Updated render.yaml with solshot.gg origins, and a clear DNS setup checklist for John.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-todo-completion/02-RESEARCH.md

Key references:
- `render.yaml` — contains CORS_ORIGINS env var for the server
- Domain: solshot.gg (registered, not yet pointed)
- Client deploy target: Vercel
- Server deploy target: Render
- Research Pattern 4: Vercel requires A record for apex, CNAME for www
- Render auto-provisions HTTPS via Let's Encrypt
- Vercel auto-provisions SSL after domain is added
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CORS_ORIGINS in render.yaml and document DNS setup</name>
  <files>render.yaml</files>
  <action>
**Part A — CORS Update:**

Read `render.yaml` and find the `CORS_ORIGINS` environment variable. Append `https://solshot.gg` and `https://www.solshot.gg` to the existing comma-separated list.

For example, if current value is:
```
CORS_ORIGINS: "https://solshot-client.vercel.app"
```
Update to:
```
CORS_ORIGINS: "https://solshot-client.vercel.app,https://solshot.gg,https://www.solshot.gg"
```

Preserve any existing origins — do NOT remove the Vercel preview URL.

Note: This render.yaml value is the initial seed. After deploy, the actual Render dashboard env var value takes precedence. John will also need to update CORS_ORIGINS in the Render dashboard after the Vercel deployment URL is confirmed.

**Part B — Document DNS Setup:**

Print out the following DNS setup instructions (these will be included in the SUMMARY):

```
DNS Setup for solshot.gg
========================

PREREQUISITES:
1. Deploy client to Vercel first (if not already done)
2. In Vercel project → Settings → Domains → Add "solshot.gg" and "www.solshot.gg"
3. Vercel will display the required A record IP and CNAME value

DNS RECORDS (at your domain registrar):

Record 1 — Apex domain (solshot.gg):
  Type: A
  Name: @ (or blank)
  Value: <IP address from Vercel dashboard>
  TTL: 3600

Record 2 — WWW subdomain:
  Type: CNAME
  Name: www
  Value: cname.vercel-dns.com.
  TTL: 3600

AFTER DNS PROPAGATION:
1. Vercel auto-provisions Let's Encrypt SSL — no manual cert needed
2. Verify: visit https://solshot.gg and https://www.solshot.gg — both should load
3. Update CORS_ORIGINS in Render dashboard to include:
   https://solshot.gg,https://www.solshot.gg
   (in addition to any Vercel preview URLs)

VERIFICATION:
- dig solshot.gg A → should return Vercel IP
- dig www.solshot.gg CNAME → should return cname.vercel-dns.com
- curl -I https://solshot.gg → should return 200
```
  </action>
  <verify>
1. `grep "solshot.gg" render.yaml` confirms the domain is in CORS_ORIGINS
2. Existing CORS origins are preserved (not deleted)
3. DNS instructions are printed in the task output
  </verify>
  <done>render.yaml CORS_ORIGINS updated with solshot.gg domains. DNS setup instructions documented for John.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Set DNS records and verify domain resolution</name>
  <action>
John completes the following steps:

1. **Deploy client to Vercel** (if not already done):
   - Connect the SolShot repo to a Vercel project
   - Set root directory to `client/`
   - Set build command: `npx react-app-rewired build`
   - Set output directory: `build`

2. **Add domain in Vercel**:
   - Vercel project → Settings → Domains → Add `solshot.gg` and `www.solshot.gg`
   - Note the A record IP address that Vercel provides

3. **Set DNS records at your domain registrar**:
   - A record: `@` → Vercel IP (from step 2)
   - CNAME record: `www` → `cname.vercel-dns.com.`

4. **Wait for propagation** (minutes to 48 hours):
   - Check progress: https://whatsmydns.net/#A/solshot.gg

5. **Verify HTTPS**:
   - Visit https://solshot.gg — should load the game client
   - Visit https://www.solshot.gg — should redirect to or serve the same content

6. **Update Render CORS**:
   - Render dashboard → your SolShot server → Environment → CORS_ORIGINS
   - Add `https://solshot.gg,https://www.solshot.gg` to the value
   - Save and redeploy

7. **Final test**:
   - Open https://solshot.gg in browser
   - Connect wallet
   - Confirm WebSocket connection to server succeeds (no CORS errors in console)
  </action>
  <how-to-verify>
1. `curl -I https://solshot.gg` returns HTTP 200 with valid SSL
2. `curl -I https://www.solshot.gg` returns HTTP 200 or 301 redirect
3. Browser console on solshot.gg shows no CORS errors
4. Wallet connect succeeds from solshot.gg domain
  </how-to-verify>
  <resume-signal>Type "dns done" when solshot.gg resolves and loads the game client, or describe any issues (DNS not propagated, SSL error, CORS error, etc.).</resume-signal>
</task>

</tasks>

<verification>
1. render.yaml CORS_ORIGINS contains `solshot.gg` and `www.solshot.gg`
2. DNS A record and CNAME are set at registrar
3. https://solshot.gg loads the game client over HTTPS
4. WebSocket connections from solshot.gg to the Render server succeed without CORS errors
</verification>

<success_criteria>
solshot.gg resolves to the Vercel-hosted game client over HTTPS. The Render server accepts requests from the solshot.gg origin. Both apex and www subdomains work correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-todo-completion/02-05-SUMMARY.md`
</output>
