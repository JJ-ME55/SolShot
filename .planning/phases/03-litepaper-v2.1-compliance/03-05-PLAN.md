---
phase: 03-litepaper-v2.1-compliance
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-litepaper-v2.1-compliance/TREASURY-GOVERNANCE.md
autonomous: true

must_haves:
  truths:
    - "Treasury multisig governance requirement is documented for the operations team"
    - "Document specifies that no code changes are needed — only env var configuration"
  artifacts:
    - path: ".planning/phases/03-litepaper-v2.1-compliance/TREASURY-GOVERNANCE.md"
      provides: "Treasury governance documentation for LP-09"
      contains: "TREASURY_WALLET"
  key_links: []
---

<objective>
Document the treasury multisig governance requirement (LP-09).

Purpose: Litepaper v2.1 specifies that the 7% treasury fee should flow to a multisig wallet for governance. No code changes are needed — the escrow program already routes 7% to the configured TREASURY_WALLET address. This plan creates the governance documentation so the operations team knows what to configure when ready.

Output: TREASURY-GOVERNANCE.md documenting the requirement, recommended solution (Squads Protocol), and env var to set.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-litepaper-v2.1-compliance/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create treasury governance documentation</name>
  <files>.planning/phases/03-litepaper-v2.1-compliance/TREASURY-GOVERNANCE.md</files>
  <action>
  Create `.planning/phases/03-litepaper-v2.1-compliance/TREASURY-GOVERNANCE.md` with the following content:

  ```markdown
  # Treasury Multisig Governance — LP-09

  ## Requirement

  Litepaper v2.1 specifies: "7% treasury fee -> multisig wallet governance."

  The escrow program (`programs/solshot-escrow/src/lib.rs`) hardcodes a 90/7/3 BPS split:
  - 90% -> Winner
  - 7% -> Treasury (platform revenue)
  - 3% -> Ops wallet (running costs)

  The treasury wallet address is read from the `TREASURY_WALLET` environment variable in `server/services/solana.js`.

  ## Current State

  - Treasury fee routing: IMPLEMENTED (in escrow program)
  - Multisig wallet: NOT CONFIGURED
  - `TREASURY_WALLET` env var: not set (defaults to null in dev)

  ## Action Required (Operations)

  1. Create a multisig wallet using Squads Protocol (https://squads.so/)
     - Squads is the ecosystem standard for Solana multisig governance
     - Configure with appropriate signers (team members with authority)
     - Set threshold (e.g., 2-of-3 for initial governance)

  2. Set the `TREASURY_WALLET` environment variable to the Squads multisig address:
     - In Render dashboard (production): Settings -> Environment -> Add `TREASURY_WALLET`
     - In `.env` (local development): `TREASURY_WALLET=<squads_multisig_address>`

  3. Verify the treasury receives 7% of each settled match by checking the multisig transaction history on Solscan

  ## No Code Changes Needed

  The escrow program already routes 7% to whatever address is in `TREASURY_WALLET`. This is purely an operations/governance configuration task. The code is ready — just set the env var.

  ## Timeline

  - Set up multisig BEFORE mainnet deployment (Phase 6)
  - Can be deferred for devnet testing (treasury address is optional in dev mode)
  ```
  </action>
  <verify>File exists at `.planning/phases/03-litepaper-v2.1-compliance/TREASURY-GOVERNANCE.md` and contains "TREASURY_WALLET" and "Squads Protocol".</verify>
  <done>Treasury governance documentation exists. Clearly states no code changes needed. Provides actionable steps for operations team. References Squads Protocol as recommended solution.</done>
</task>

</tasks>

<verification>
1. TREASURY-GOVERNANCE.md exists in the phase directory
2. Document covers: requirement, current state, action required, no code changes
3. References TREASURY_WALLET env var and Squads Protocol
</verification>

<success_criteria>
- LP-09 requirement is documented and actionable
- Operations team has clear steps to configure multisig
- No code changes were made (correct — none are needed)
</success_criteria>

<output>
After completion, create `.planning/phases/03-litepaper-v2.1-compliance/03-05-SUMMARY.md`
</output>
