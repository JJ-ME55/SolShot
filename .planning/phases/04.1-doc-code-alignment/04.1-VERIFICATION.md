---
phase: 04.1-doc-code-alignment
verified: 2026-02-22T22:09:45Z
status: passed
score: 20/20 must-haves verified
re_verification: false
---

# Phase 4.1: Doc-Code Alignment Verification Report

**Phase Goal:** Code matches all 39 litepaper/doc decisions -- deposit countdown timer implemented, permissionless reclaim instruction added to escrow program, disconnect handler uses HP-based settlement for connection drops, dead code removed
**Verified:** 2026-02-22T22:09:45Z
**Status:** passed
**Re-verification:** No -- initial verification

---

## Goal Achievement

### Observable Truths

| #  | Truth | Status | Evidence |
|----|-------|--------|---------|
| 1  | After both players join a wagered match, a 2-minute deposit countdown starts on the server | VERIFIED | `DEPOSIT_TIMEOUT_MS = 120_000` at line 55; `depositTimers[roomId] = setTimeout` set in both joinRoom (line ~1058) and joinQueue (line ~1321) paths |
| 2  | If only one player deposits before expiry, cancelMatchEscrow is called and both receive escrowDepositTimeout | VERIFIED | Timeout callback checks both deposits, calls `cancelMatchEscrow(roomId, p1wallet, p2wallet)`, emits `escrowDepositTimeout` |
| 3  | Both clients receive depositDeadlineMs in the escrowDeposit event | VERIFIED | `depositDeadline` computed once before both emits; `depositDeadlineMs: depositDeadline` in both host and joiner `escrowDeposit` events in both code paths |
| 4  | The deposit timer is cleared when both deposits are confirmed (escrowDepositConfirm handler) | VERIFIED | `clearTimeout(depositTimers[rid])` inside `if (hostDeposited && playerDeposited)` block at line ~1792 |
| 5  | The deposit timer is cleared when the room is removed for any reason (removeRoom) | VERIFIED | `removeRoom()` clears `depositTimers[roomId]` alongside shopTimers and turnTimers at line ~225 |
| 6  | When a player disconnects mid-match (reconnect_timeout), the server checks HP and roundWins | VERIFIED | `cleanupRoom()` checks reason === reconnect_timeout then runs roundWins -> HP -> scores chain at lines 602-629 |
| 7  | If the disconnected player is behind on rounds or HP, the opponent wins the wager | VERIFIED | Decision chain awards win to player ahead on roundWins, then HP, then score; default is opponent wins |
| 8  | If both players are genuinely even, the server refunds both via cancelMatchEscrow | VERIFIED | `dScore === oScore` -> `shouldRefund = true` -> cancelMatchEscrow + matchSettled { type: refund, reason: even_disconnect } |
| 9  | Intentional quits (leaveRoom) still unconditionally forfeit to opponent -- no HP check | VERIFIED | `cleanupRoom(client, io, 'leave')` from leaveRoom; reconnect_timeout guard never entered; default winnerWallet = opponentWallet |
| 10 | server/services/raydium.js is deleted | VERIFIED | File does not exist; grep finds no raydium references anywhere in server/ or client/ |
| 11 | A permissionless_reclaim instruction exists in lib.rs that anyone can call after 48 hours | VERIFIED | `pub fn permissionless_reclaim(ctx: Context<PermissionlessReclaim>)` at line 381; no authority/player constraint on caller |
| 12 | permissionless_reclaim requires no specific signer role -- any wallet can trigger it | VERIFIED | `PermissionlessReclaim` struct has `caller: Signer<'info>` with no `has_one` or identity constraint |
| 13 | permissionless_reclaim uses 2x TIMEOUT_SECONDS (172800s = 48h) from activated_at or created_at | VERIFIED | `PERMISSIONLESS_RECLAIM_TIMEOUT: i64 = TIMEOUT_SECONDS * 2` (line 23); uses activated_at if > 0 else created_at; checked_add with constant |
| 14 | permissionless_reclaim refunds deposited amounts to respective players and closes the PDA | VERIFIED | Refund logic identical to cancel_match; `close = caller` in `PermissionlessReclaim` struct |
| 15 | permissionless_reclaim is SEPARATE from cancel_match | VERIFIED | `PermissionlessReclaim` struct has NO config account; cancel_match still includes config account with authority check |
| 16 | The caller of permissionless_reclaim receives the rent lamports | VERIFIED | `close = caller` in `PermissionlessReclaim` struct accounts definition |
| 17 | A TooEarlyToReclaim error is returned if called before 48 hours have elapsed | VERIFIED | `require!(Clock::get()?.unix_timestamp > reclaim_deadline, EscrowError::TooEarlyToReclaim)` at lines 408-411 |
| 18 | permissionless_reclaim cannot be called on Settled or Cancelled escrows | VERIFIED | `require!(escrow_state != MatchState::Settled && escrow_state != MatchState::Cancelled, EscrowError::InvalidState)` at lines 390-394 |
| 19 | The IDL at server/idl/solshot_escrow.json includes the new instruction | VERIFIED | IDL contains name: permissionless_reclaim with correct accounts (no config); TooEarlyToReclaim error code 6019 present |
| 20 | escrow.js has a wrapper function for permissionless_reclaim | VERIFIED | `export async function permissionlessReclaimEscrow(matchId, playerOneAddress, playerTwoAddress)` at line 478; calls `.permissionlessReclaim()` via Anchor camelCase; SystemProgram imported |

**Score:** 20/20 truths verified

---

## Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `server/socket-io/main.js` | Deposit timer + HP disconnect settlement | VERIFIED | depositTimers map, DEPOSIT_TIMEOUT_MS, depositDeadlineMs, HP decision chain all present and substantive |
| `programs/solshot-escrow/src/lib.rs` | permissionless_reclaim instruction | VERIFIED | Function + PermissionlessReclaim struct + TooEarlyToReclaim error all present |
| `server/idl/solshot_escrow.json` | Updated IDL with new instruction | VERIFIED | permissionless_reclaim instruction + TooEarlyToReclaim error code 6019 in IDL |
| `server/services/escrow.js` | permissionlessReclaimEscrow wrapper + SystemProgram import | VERIFIED | Both present; substantive real implementation |
| `server/services/raydium.js` | Must NOT exist | VERIFIED | File deleted; no imports of raydium anywhere |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| main.js joinRoom escrow block | depositTimers | setTimeout(DEPOSIT_TIMEOUT_MS) | WIRED | Timer set immediately after escrowDeposit emits; deadline included in emit payload |
| main.js joinQueue escrow block | depositTimers | setTimeout(DEPOSIT_TIMEOUT_MS) | WIRED | Same pattern as joinRoom; both code paths covered |
| depositTimers timeout | cancelMatchEscrow | wallet lookup + isEscrowEnabled() guard | WIRED | Calls cancelMatchEscrow if wallets present and escrow enabled |
| depositTimers timeout | escrowDepositTimeout emit | io.sockets.in(roomId).emit | WIRED | Event emitted after cancel attempt, before removeRoom |
| escrowDepositConfirm handler | depositTimers clear | clearTimeout(depositTimers[rid]) | WIRED | Cleared inside if (hostDeposited && playerDeposited) block |
| removeRoom() | depositTimers clear | clearTimeout(depositTimers[roomId]) | WIRED | Cleared as safety net alongside shopTimers/turnTimers |
| cleanupRoom (reconnect_timeout) | HP decision chain | reason === reconnect_timeout guard | WIRED | Decision chain runs only for involuntary drops; leave/disconnect default to forfeit |
| HP decision chain | cancelMatchEscrow (even case) | shouldRefund = true | WIRED | cancelMatchEscrow called when all metrics are equal |
| permissionless_reclaim fn | MatchCancelled event | emit!(MatchCancelled) | WIRED | Same emit as cancel_match; refunds deposited players |
| permissionlessReclaimEscrow | Anchor program | .permissionlessReclaim() camelCase | WIRED | Anchor SDK converts IDL snake_case to camelCase at runtime |

---

## Requirements Coverage

| Requirement | Status | Notes |
|-------------|--------|-------|
| DCA-01: Deposit countdown timer | SATISFIED | 2-minute timer in both join paths; deadline in client event; cleared on double-confirm and removeRoom |
| DCA-02: Permissionless reclaim instruction | SATISFIED | On-chain instruction + IDL + server wrapper all verified |
| DCA-03: HP-based disconnect settlement | SATISFIED | reconnect_timeout uses roundWins->HP->scores chain; leave always forfeits |
| DCA-04: Dead Raydium code removed | SATISFIED | File deleted, no imports anywhere |

---

## Anti-Patterns Found

None blocking. Spot check of modified sections found no TODO/placeholder patterns. The HP default of 250 (starting HP) is intentional and correct -- prevents a disconnecting player from appearing to have 0 HP before the round begins.

---

## Human Verification Required

None -- all success criteria are structurally verifiable in code. The following are pre-deployment items only (not code gaps):

1. **Fresh devnet deploy needed** -- lib.rs changed, so the on-chain program at CqvRC6mSJe2CrBtENVfCEPkgRW3WwxLSL9C1hgXz7GtD does not yet have permissionless_reclaim. This is a deployment step, not a code gap.

2. **Anchor test suite** -- tests/solshot-escrow.ts should add coverage for permissionless_reclaim and TooEarlyToReclaim. Noted in SUMMARY as pending local validator setup.

---

## Summary

All 20 must-have truths verified against actual code. No stubs, no orphaned artifacts, no broken wiring.

**Plan 01 (DCA-01 + DCA-03 + DCA-04):**
- `depositTimers` map and `DEPOSIT_TIMEOUT_MS = 120_000` added alongside existing timer maps
- Both escrow deposit paths (joinRoom + joinQueue) emit `depositDeadlineMs` and start countdown
- Countdown clears on confirmed double-deposit (`escrowDepositConfirm`) and on `removeRoom`
- Timeout callback guards against spurious cancellation when both deposits already confirmed
- HP decision chain (roundWins -> HP -> scores -> refund) runs only for `reason === 'reconnect_timeout'`
- `leave` and `disconnect` unconditionally forfeit to opponent -- no HP check
- `raydium.js` deleted; no references remain anywhere in codebase

**Plan 02 (DCA-02):**
- `permissionless_reclaim` in lib.rs: no config account, any Signer can call, 2x timeout (172800s), defense-in-depth state transition before transfers, `close = caller` for rent incentive
- `TooEarlyToReclaim` error code in `EscrowError` enum
- IDL rebuilt with instruction and correct accounts (no config account present)
- `permissionlessReclaimEscrow()` wrapper in escrow.js: uses `provider.wallet.publicKey` as caller, `SystemProgram` imported

---

_Verified: 2026-02-22T22:09:45Z_
_Verifier: Claude (gsd-verifier)_
