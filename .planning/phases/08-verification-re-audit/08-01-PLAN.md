---
phase: 08-verification-re-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .audit/STATE.json
  - .audit/FINAL_REPORT.md
  - .audit/ARCHITECTURE.md
  - .audit/STRATEGIES.md
  - .audit/HOT_SPOTS.md
autonomous: false

must_haves:
  truths:
    - "SOS re-audit covers the hardened lib.rs (Phase 1 GlobalConfig PDA, initialize_config, permissionless_reclaim, checked arithmetic)"
    - "SOS FINAL_REPORT.md shows 0 CRITICAL and 0 HIGH confirmed findings"
    - "Any new CRITICAL or HIGH finding discovered during re-audit is fixed inline before gate passes"
  artifacts:
    - path: ".audit/FINAL_REPORT.md"
      provides: "Fresh SOS audit report on hardened on-chain program"
      contains: "FINAL_REPORT"
    - path: ".audit/STATE.json"
      provides: "Audit state showing completed pipeline"
  key_links:
    - from: "programs/solshot-escrow/src/lib.rs"
      to: ".audit/FINAL_REPORT.md"
      via: "The Fortress audit pipeline"
      pattern: "lib\\.rs"
---

<objective>
Run a fresh SOS (The Fortress) security audit on the hardened on-chain escrow program (`programs/solshot-escrow/src/lib.rs`) and verify that all CRITICAL and HIGH findings from the original audit are resolved. This is not a targeted re-check — it is a full audit from Phase 0 on the current code.

Purpose: Certify that the on-chain escrow program is free of CRITICAL and HIGH vulnerabilities after Phases 1, 4.1 remediation work. This is one of three parallel audit gates required before mainnet deployment.

Output: A fresh `.audit/FINAL_REPORT.md` with 0 CRITICAL and 0 HIGH findings (excluding H029 documented as ACCEPTED RISK).
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verification-re-audit/08-CONTEXT.md
@.planning/phases/08-verification-re-audit/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Audit Preparation — Clean SOS State</name>
  <files>.audit/STATE.json</files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean (NOT SolShot).

    1. Delete `.audit/STATE.json` to force The Fortress to start a clean audit pipeline instead of resuming the pre-remediation run. Run:
       ```
       rm .audit/STATE.json
       ```

    2. Verify the current program source is the hardened version by checking for Phase 1 markers:
       - `programs/solshot-escrow/src/lib.rs` should contain `GlobalConfig` PDA, `initialize_config` instruction, `permissionless_reclaim` instruction, and `checked_mul`/`checked_div` arithmetic.
       - If any of these are missing, STOP — you are on the wrong codebase or Phase 1 was not applied.

    3. Document the SOS audit scope for the user:
       - Scope: `programs/solshot-escrow/src/lib.rs` ONLY (approximately 750 LOC)
       - The Fortress should NOT analyze server/ or client/ code (DB owns that scope)
       - The pre-remediation SOS found 7 CRITICAL and 6 HIGH findings in the original escrow program
       - Expected outcome: 0 CRITICAL and 0 HIGH after Phase 1 + 4.1 fixes

    Do NOT attempt to invoke The Fortress skill — that is the user's job in the next task.
  </action>
  <verify>
    - `.audit/STATE.json` no longer exists (deleted)
    - `grep -c "GlobalConfig" programs/solshot-escrow/src/lib.rs` returns >= 1
    - `grep -c "initialize_config" programs/solshot-escrow/src/lib.rs` returns >= 1
    - `grep -c "permissionless_reclaim" programs/solshot-escrow/src/lib.rs` returns >= 1
    - `grep -c "checked_mul\|checked_div\|checked_add\|checked_sub" programs/solshot-escrow/src/lib.rs` returns >= 1
  </verify>
  <done>STATE.json deleted; lib.rs confirmed as hardened version with all Phase 1 markers present; user briefed on scope.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run The Fortress (SOS) Audit</name>
  <action>
    The user must manually run The Fortress security audit against the on-chain escrow program.

    **Instructions for the user:**

    1. Open a NEW Claude Code session (fresh context window) with working directory set to `C:/Users/johnk/SolShot-clean`
    2. Run the SOS audit skill. The exact invocation depends on how The Fortress is configured, but typically:
       - Use the `/SOS:scan` command (or equivalent slash command for The Fortress)
       - Scope: `programs/solshot-escrow/src/lib.rs`
       - This is a FULL audit (not :verify mode) — let it run all phases: preflight, context, strategies, investigate, report
    3. The audit will take significant time (potentially hours) as it runs through all 5 phases
    4. Wait for the audit to complete and produce `.audit/FINAL_REPORT.md`
    5. Return to this plan's execution session and type "SOS audit complete" to continue

    **Scope reminder:** SOS covers ONLY the on-chain Anchor program. Server/client findings belong to the DB audit (Plan 08-02).

    **If the skill scope-creeps into server code:** Redirect it — the on-chain program is the only target for SOS.
  </action>
  <resume-signal>Type "SOS audit complete" after The Fortress finishes and .audit/FINAL_REPORT.md is updated</resume-signal>
</task>

<task type="auto">
  <name>Task 3: SOS Gate Check — Verify 0 CRITICAL / 0 HIGH</name>
  <files></files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean.

    1. **Verify report freshness** — confirm FINAL_REPORT.md is from the re-audit, NOT the pre-remediation report:
       a. Check `.audit/STATE.json` exists (it was deleted in Task 1; The Fortress recreates it during a fresh run). If STATE.json does NOT exist, the audit did not complete — STOP and escalate.
       b. Read `.audit/STATE.json` and check the `audit_id` or `git_ref` field — it should reference a commit AFTER the Phase 1 hardening (not the original pre-remediation commit).
       c. Read the first 30 lines of `.audit/FINAL_REPORT.md` and check the report date in the document header. The date MUST be 2026-02-23 or later (today or after). If the date is 2026-02-14 or earlier, this is the STALE pre-remediation report — STOP and escalate to user ("The Fortress did not overwrite FINAL_REPORT.md — the report is stale").
       d. Additionally, check that the report body references Phase 1 constructs (e.g., `GlobalConfig`, `initialize_config`) — a fresh audit of the hardened code will mention these; the old report will not.

    2. Read the full `.audit/FINAL_REPORT.md` (now confirmed fresh).

    3. Parse the findings and classify by severity:
       - Count CRITICAL findings
       - Count HIGH findings
       - Count MEDIUM findings
       - Count LOW findings

    4. Apply the gate check:
       - **PASS** if: 0 CRITICAL and 0 HIGH confirmed findings
         - H029 (outcome verification / unverifiable winner oracle) is pre-authorized as ACCEPTED RISK — if it appears as HIGH, it does NOT count against the gate. Document it as "Deferred to v1.2 — requires on-chain oracle mechanism."
       - **FAIL** if: Any new CRITICAL or HIGH finding exists (besides H029)
         - If FAIL: Read the finding details, identify the fix needed, implement the fix in `programs/solshot-escrow/src/lib.rs`, verify the fix resolves the finding, and document what was fixed. Then re-read the report or re-run the relevant investigation to confirm resolution.
         - After inline fix: Re-verify gate condition.

    5. Produce a gate summary:
       ```
       SOS RE-AUDIT GATE: [PASS/FAIL]
       CRITICAL: [N] (target: 0)
       HIGH: [N] (target: 0, excluding accepted risks)
       MEDIUM: [N] (documented, not blocking)
       LOW: [N] (documented, not blocking)
       Accepted Risks: [list with justification]
       Report Date: [date from FINAL_REPORT.md header]
       STATE.json audit_id: [value from STATE.json]
       ```

    6. If PASS: This plan is complete. The SOS audit gate is satisfied.
       If FAIL after inline fix attempt: Escalate to user — a new finding requires discussion.
  </action>
  <verify>
    - `.audit/STATE.json` exists (recreated by The Fortress during re-audit)
    - `.audit/FINAL_REPORT.md` header date is 2026-02-23 or later (not the pre-remediation report dated 2026-02-14)
    - FINAL_REPORT.md body references Phase 1 constructs (GlobalConfig, initialize_config)
    - Gate summary shows 0 CRITICAL and 0 HIGH (excluding accepted risks)
    - Any H029-equivalent finding is documented as ACCEPTED RISK with justification
  </verify>
  <done>SOS re-audit complete; FINAL_REPORT.md confirmed fresh (post-remediation date, references hardened code); report shows 0 CRITICAL and 0 HIGH findings; gate check PASSES; accepted risks documented.</done>
</task>

</tasks>

<verification>
- `.audit/FINAL_REPORT.md` is a fresh report (not the 2026-02-14 pre-remediation version)
- Report covers the hardened lib.rs with GlobalConfig PDA, initialize_config, permissionless_reclaim
- 0 CRITICAL confirmed findings
- 0 HIGH confirmed findings (H029 classified as ACCEPTED RISK does not count)
- Any new MEDIUM/LOW findings are documented but do not block the gate
</verification>

<success_criteria>
The SOS security audit on the hardened on-chain escrow program confirms 0 CRITICAL and 0 HIGH findings. The only HIGH-severity accepted risk is H029 (outcome verification), documented with v1.2 deferral justification.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-re-audit/08-01-SUMMARY.md`
</output>