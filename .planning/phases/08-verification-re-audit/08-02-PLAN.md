---
phase: 08-verification-re-audit
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .bulwark/FINAL_REPORT.md
autonomous: false

must_haves:
  truths:
    - "DB re-audit covers the hardened server and client code (Phases 2-7 fixes: auth guards, deposit verification, settlement recovery, CSP, key management, token persistence, infrastructure)"
    - "DB FINAL_REPORT.md shows 0 CRITICAL and 0 HIGH confirmed findings"
    - "Any new CRITICAL or HIGH finding discovered during re-audit is fixed inline before gate passes"
  artifacts:
    - path: ".bulwark/FINAL_REPORT.md"
      provides: "Fresh DB audit report on hardened server and client"
      contains: "FINAL_REPORT"
  key_links:
    - from: "server/"
      to: ".bulwark/FINAL_REPORT.md"
      via: "Dinh's Bulwark audit pipeline"
      pattern: "server"
    - from: "client/"
      to: ".bulwark/FINAL_REPORT.md"
      via: "Dinh's Bulwark audit pipeline"
      pattern: "client"
---

<objective>
Run a fresh DB (Dinh's Bulwark) security audit on the hardened server and client code and verify that all CRITICAL and HIGH findings from the original audit are resolved. This is a full audit from scratch — not a targeted re-check.

Purpose: Certify that the Express/Socket.IO server and React client are free of CRITICAL and HIGH vulnerabilities after Phases 2-7 remediation. This is one of three parallel audit gates required before mainnet deployment.

Output: A fresh `.bulwark/FINAL_REPORT.md` with 0 CRITICAL and 0 HIGH findings (excluding H029 claimedMatchIds outcome verification documented as ACCEPTED RISK / Deferred to v1.2).
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verification-re-audit/08-CONTEXT.md
@.planning/phases/08-verification-re-audit/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Audit Preparation — Verify Hardened Codebase</name>
  <files></files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean (NOT SolShot).

    1. Confirm `.bulwark/` does NOT exist in SolShot-clean (the DB skill will create it on first run):
       ```
       ls .bulwark/ 2>/dev/null || echo ".bulwark/ does not exist — DB will create it"
       ```

    2. Verify the server code contains Phase 2-7 hardening markers by spot-checking key files:
       - `server/services/keys.js` exists (Phase 4 — centralized key management)
       - `server/socket-io/main.js` contains `requireAuth` (Phase 3 — auth guards)
       - `server/services/escrow.js` contains `getEscrowState` (Phase 2 — on-chain deposit verification)
       - `server/index.js` contains `requireAdminKey` or similar admin guard (Phase 7 — endpoint auth)
       - `client/src/contexts/SolShotWalletContext.js` contains `validateEscrowTransaction` (Phase 5 — TX validation)
       - `client/public/index.html` contains `Content-Security-Policy` (Phase 5 — CSP)

    3. If any of these markers are missing, STOP — the codebase may not have all Phase 2-7 fixes applied.

    4. Document the DB audit scope for the user:
       - Scope: `server/` and `client/` directories (Express/Socket.IO server + React client)
       - DB should NOT analyze `programs/solshot-escrow/src/lib.rs` (SOS owns that scope)
       - The pre-remediation DB found 8 CRITICAL and 17 HIGH findings
       - Expected outcome: 0 CRITICAL and 0 HIGH after Phases 2-7 fixes
       - Known accepted risk: H029 (claimedMatchIds / outcome verification) — Deferred to v1.2

    Do NOT attempt to invoke the DB skill — that is the user's job in the next task.
  </action>
  <verify>
    - `.bulwark/` does not exist (confirmed clean start)
    - `grep -c "requireAuth" server/socket-io/main.js` returns >= 1
    - `grep -c "keys.js\|getServerKeypair" server/services/escrow.js` returns >= 1
    - `grep -c "validateEscrowTransaction" client/src/contexts/SolShotWalletContext.js` returns >= 1
  </verify>
  <done>No stale .bulwark/ state exists; server and client confirmed as hardened versions with Phase 2-7 markers present; user briefed on scope.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run Dinh's Bulwark (DB) Audit</name>
  <action>
    The user must manually run the DB security audit against the server and client code.

    **Instructions for the user:**

    1. Open a NEW Claude Code session (fresh context window) with working directory set to `C:/Users/johnk/SolShot-clean`
    2. Run the DB audit skill. Typically:
       - Use the `/DB:scan` command (or equivalent slash command for Dinh's Bulwark)
       - Scope: `server/` and `client/` directories
       - This is a FULL audit (not :verify mode) — let it run all phases through to final report
    3. The audit will take significant time (potentially hours) as it runs its full pipeline
    4. Wait for the audit to complete and produce `.bulwark/FINAL_REPORT.md`
    5. Return to this plan's execution session and type "DB audit complete" to continue

    **Scope reminder:** DB covers ONLY the off-chain server and client code. On-chain program findings belong to the SOS audit (Plan 08-01).

    **Known accepted risk:** H029 (claimedMatchIds / outcome verification) is pre-authorized as ACCEPTED RISK — Deferred to v1.2. If DB flags this, it does not block the gate.
  </action>
  <resume-signal>Type "DB audit complete" after Dinh's Bulwark finishes and .bulwark/FINAL_REPORT.md exists</resume-signal>
</task>

<task type="auto">
  <name>Task 3: DB Gate Check — Verify 0 CRITICAL / 0 HIGH</name>
  <files></files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean.

    1. Read `.bulwark/FINAL_REPORT.md` (the freshly generated report).

    2. Parse the findings and classify by severity:
       - Count CRITICAL findings
       - Count HIGH findings
       - Count MEDIUM findings
       - Count LOW findings

    3. Apply the gate check:
       - **PASS** if: 0 CRITICAL and 0 HIGH confirmed findings
         - H029 (claimedMatchIds / outcome verification) is pre-authorized as ACCEPTED RISK — if it appears as HIGH, it does NOT count against the gate. Document as "Deferred to v1.2 — requires protocol-level oracle design; mitigated by server-authoritative game logic."
         - H030 (dev mode prestige bypass) if flagged — document as "Mitigated (not fixed) — dev mode only, production env var enforces on-chain verification."
         - H060 (horizontal scaling) if flagged — document as "Mitigated (not fixed) — single instance deployment, not exploitable; v1.2 consideration."
       - **FAIL** if: Any new CRITICAL or HIGH finding exists (besides pre-authorized accepted risks)
         - If FAIL: Read the finding details, identify the fix needed, implement the fix in the appropriate server or client file, verify it resolves the finding. Then re-verify gate condition.
         - After inline fix: Re-check the report or re-run the relevant section.

    4. Produce a gate summary:
       ```
       DB RE-AUDIT GATE: [PASS/FAIL]
       CRITICAL: [N] (target: 0)
       HIGH: [N] (target: 0, excluding accepted risks)
       MEDIUM: [N] (documented, not blocking)
       LOW: [N] (documented, not blocking)
       Accepted Risks: [list with justification]
       ```

    5. If PASS: This plan is complete. The DB audit gate is satisfied.
       If FAIL after inline fix attempt: Escalate to user.
  </action>
  <verify>
    - `.bulwark/FINAL_REPORT.md` exists and contains fresh findings
    - Gate summary shows 0 CRITICAL and 0 HIGH (excluding accepted risks)
    - Accepted risks are documented with justification and category
  </verify>
  <done>DB re-audit complete; FINAL_REPORT.md shows 0 CRITICAL and 0 HIGH findings; gate check PASSES; accepted risks documented.</done>
</task>

</tasks>

<verification>
- `.bulwark/FINAL_REPORT.md` exists as a fresh report on the hardened codebase
- Report covers server and client (not on-chain program)
- 0 CRITICAL confirmed findings
- 0 HIGH confirmed findings (H029 and any other pre-authorized findings classified as ACCEPTED RISK do not count)
- Any new MEDIUM/LOW findings are documented but do not block the gate
</verification>

<success_criteria>
The DB security audit on the hardened server and client confirms 0 CRITICAL and 0 HIGH findings. Pre-authorized accepted risks (H029, H030, H060) are documented with justification and deferral category.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-re-audit/08-02-SUMMARY.md`
</output>
