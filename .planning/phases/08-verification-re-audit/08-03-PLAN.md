---
phase: 08-verification-re-audit
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .bok/reports/2026-02-23-report.md
autonomous: false

must_haves:
  truths:
    - "BOK re-verification covers the updated lib.rs including new instructions (initialize_config, permissionless_reclaim) and checked arithmetic"
    - "BOK report shows 0 invariant failures"
    - "Coverage gaps from original run are documented with status (fixed or accepted)"
  artifacts:
    - path: ".bok/reports/2026-02-23-report.md"
      provides: "Fresh BOK math verification report on hardened program"
      contains: "report"
  key_links:
    - from: "programs/solshot-escrow/src/lib.rs"
      to: ".bok/"
      via: "BOK proptest verification"
      pattern: "lib\\.rs"
---

<objective>
Run a fresh BOK mathematical verification on the hardened on-chain escrow program to confirm that all arithmetic invariants hold and the 8 original coverage gaps are addressed or documented. BOK runs in DEGRADED mode (proptest only — Kani blocked by McAfee).

Purpose: Certify that all financial arithmetic (BPS splits, lamport transfers, overflow guards) in the updated program is mathematically sound. The program now has additional instructions (initialize_config, permissionless_reclaim) and checked arithmetic from Phase 1 that need BOK coverage.

Output: A fresh BOK report at `.bok/reports/2026-02-23-report.md` with 0 invariant failures and documented coverage gap status.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-verification-re-audit/08-CONTEXT.md
@.planning/phases/08-verification-re-audit/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-Audit Preparation — Verify BOK Scope</name>
  <files></files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean (NOT SolShot).

    1. Confirm `.bok/` does NOT exist in SolShot-clean (BOK will create it on first run):
       ```
       ls .bok/ 2>/dev/null || echo ".bok/ does not exist — BOK will create it"
       ```

    2. Verify the program source contains Phase 1 arithmetic improvements that BOK should cover:
       - `programs/solshot-escrow/src/lib.rs` contains `checked_mul`, `checked_div`, `checked_add`, `checked_sub` (Phase 1 checked arithmetic)
       - `programs/solshot-escrow/src/lib.rs` contains `initialize_config` (new instruction needing BOK coverage)
       - `programs/solshot-escrow/src/lib.rs` contains `permissionless_reclaim` (Phase 4.1 — new instruction needing BOK coverage for timeout arithmetic)

    3. Document the BOK audit scope for the user:
       - Scope: `programs/solshot-escrow/src/lib.rs` — all arithmetic paths
       - Mode: DEGRADED (proptest only — Kani is blocked by McAfee LiveSafe on Windows)
       - The original BOK found 0 invariant failures across 24 invariants / 39 tests, with 8 coverage gaps
       - Expected outcome: 0 invariant failures; new invariants covering initialize_config and permissionless_reclaim
       - Coverage gaps: Document whether original 8 gaps are now addressed by Phase 1 changes:
         - GAP-001: Arithmetic overflow in BPS calc (FIXED — checked arithmetic)
         - GAP-002: Minimum wager boundary (FIXED — MIN_WAGER_LAMPORTS constant)
         - GAP-003: Treasury/ops account validation (FIXED — config PDA constraints)
         - GAP-004: Maximum wager boundary (FIXED — MAX_WAGER_LAMPORTS constant)
         - GAP-005: Concurrent match stress (Kani-only — remains DEGRADED)
         - GAP-006: Error message information leakage (FIXED — generic error codes)
         - GAP-007: Fee basis point rounding (FIXED — checked arithmetic with u128)
         - GAP-008: Timeout manipulation (FIXED — activated_at timestamp + permissionless_reclaim)

    Do NOT attempt to invoke the BOK skill — that is the user's job in the next task.
  </action>
  <verify>
    - `.bok/` does not exist (confirmed clean start)
    - `grep -c "checked_mul\|checked_div\|checked_add\|checked_sub" programs/solshot-escrow/src/lib.rs` returns >= 4
    - `grep -c "initialize_config" programs/solshot-escrow/src/lib.rs` returns >= 1
    - `grep -c "permissionless_reclaim" programs/solshot-escrow/src/lib.rs` returns >= 1
  </verify>
  <done>No stale .bok/ state exists; lib.rs confirmed with Phase 1 checked arithmetic and new instructions; user briefed on scope and DEGRADED mode expectations.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run BOK Mathematical Verification</name>
  <action>
    The user must manually run the BOK mathematical verification against the escrow program.

    **Instructions for the user:**

    1. Open a NEW Claude Code session (fresh context window) with working directory set to `C:/Users/johnk/SolShot-clean`
    2. Run the BOK skill. Typically:
       - Use the `/BOK:scan` command (or equivalent slash command for BOK)
       - Scope: `programs/solshot-escrow/src/lib.rs` — all arithmetic invariants
       - Mode will be DEGRADED (proptest only) due to McAfee blocking Kani/solana-test-validator
       - If proptest ALSO fails to run, document as "BOK BLOCKED" — this does not block the overall Phase 8 gate since BOK findings are coverage gaps (not exploitable bugs)
    3. Wait for BOK to complete and produce a report in `.bok/reports/`
    4. Return to this plan's execution session and type "BOK audit complete" to continue

    **If proptest is blocked:** Type "BOK blocked" — the executor will document this as a known limitation and proceed. BOK failures do not block the Phase 8 gate (original BOK found 0 exploitable bugs; gaps are hardening recommendations).

    **New instruction coverage:** The BOK run should cover new arithmetic in `initialize_config` (config PDA setup) and `permissionless_reclaim` (48h timeout calculation using `activated_at`). The original 24 invariants covered only the 4 original instructions.
  </action>
  <resume-signal>Type "BOK audit complete" or "BOK blocked" after the verification finishes or fails</resume-signal>
</task>

<task type="auto">
  <name>Task 3: BOK Gate Check — Verify 0 Invariant Failures</name>
  <files></files>
  <action>
    IMPORTANT: Working directory is C:/Users/johnk/SolShot-clean.

    **If BOK completed:**

    1. Read the BOK report (likely at `.bok/reports/2026-02-23-report.md` or similar dated file).

    2. Parse the results:
       - Count invariants tested
       - Count invariant failures
       - Count new invariants (for initialize_config, permissionless_reclaim)
       - List remaining coverage gaps

    3. Apply the gate check:
       - **PASS** if: 0 invariant failures
         - Coverage gaps are documented but do NOT block the gate
         - Kani-only gaps (GAP-005 concurrent match stress) remain as "DEGRADED mode limitation"
       - **FAIL** if: Any invariant failure exists
         - If FAIL: Read the failure details, identify the arithmetic bug, fix it in `programs/solshot-escrow/src/lib.rs`, re-verify. Then re-check.

    4. Produce a gate summary:
       ```
       BOK RE-VERIFICATION GATE: [PASS/FAIL/BLOCKED]
       Mode: DEGRADED (proptest only)
       Invariants tested: [N]
       Invariant failures: [N] (target: 0)
       New invariants (Phase 1 instructions): [N]
       Remaining coverage gaps: [list]
       Original 8 gaps status: [N] fixed, [N] remaining
       ```

    **If BOK was blocked:**

    1. Document as:
       ```
       BOK RE-VERIFICATION GATE: BLOCKED
       Reason: McAfee LiveSafe blocks proptest execution on Windows
       Mitigation: Original BOK (2026-02-21) found 0 exploitable bugs across 24 invariants / 39 tests
       Phase 1 changes: Checked arithmetic added — reduces risk further
       Risk assessment: LOW — no known arithmetic vulnerabilities; proptest verification deferred to Linux/CI environment
       ```

    2. This does NOT block the overall Phase 8 gate. BOK gaps are hardening recommendations, not exploitable vulnerabilities.
  </action>
  <verify>
    - BOK report exists in `.bok/reports/` OR blocked status is documented
    - If completed: 0 invariant failures
    - Coverage gap status documented (fixed vs remaining)
    - Gate summary produced
  </verify>
  <done>BOK re-verification complete (or documented as BLOCKED); 0 invariant failures confirmed (or blocked status documented with risk assessment); coverage gaps cataloged.</done>
</task>

</tasks>

<verification>
- BOK report exists in `.bok/reports/` (or blocked status documented)
- 0 invariant failures (if BOK ran successfully)
- New instructions (initialize_config, permissionless_reclaim) have arithmetic coverage (or documented as gap)
- Original 8 coverage gaps have documented status (fixed by Phase 1 vs remaining)
- BOK blocked status does NOT block Phase 8 overall gate
</verification>

<success_criteria>
BOK mathematical verification on the hardened escrow program confirms 0 invariant failures. Coverage gaps are documented with status. If BOK is blocked by McAfee, the limitation is documented with risk assessment referencing the original clean BOK run.
</success_criteria>

<output>
After completion, create `.planning/phases/08-verification-re-audit/08-03-SUMMARY.md`
</output>
