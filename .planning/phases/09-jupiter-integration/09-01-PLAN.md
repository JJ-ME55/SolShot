---
phase: 09-jupiter-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/public/index.html
  - server/index.js
  - server/services/jupiter-price.js
  - server/socket-io/main.js
  - server/.env.example
autonomous: true
user_setup:
  - service: jupiter-price-api
    why: "Server-side SHOT price fetching from Jupiter Price API V3"
    env_vars:
      - name: JUP_API_KEY
        source: "portal.jup.ag -> Create account -> API Keys -> Create Lite tier key (free, 60 req/min)"

must_haves:
  truths:
    - "No CSP violations in browser console when loading Plugin CDN script or calling Jupiter APIs"
    - "Server fetches SHOT/SOL price from api.jup.ag and returns a formatted price string via socket"
    - "Price service handles errors gracefully (returns null/fallback when API is down or token has no liquidity)"
  artifacts:
    - path: "server/services/jupiter-price.js"
      provides: "Jupiter Price API V3 client"
      exports: ["getShotPrice", "startPricePolling", "stopPricePolling"]
    - path: "client/public/index.html"
      provides: "Plugin CDN script tag and updated CSP meta tag"
      contains: "plugin.jup.ag"
    - path: "server/index.js"
      provides: "Updated Helmet CSP directives for Jupiter domains"
      contains: "api.jup.ag"
  key_links:
    - from: "server/socket-io/main.js"
      to: "server/services/jupiter-price.js"
      via: "getShotPrice import and getShotPrice socket handler"
      pattern: "getShotPrice"
    - from: "server/services/jupiter-price.js"
      to: "https://api.jup.ag/price/v3"
      via: "fetch with x-api-key header"
      pattern: "api\\.jup\\.ag/price/v3"
  csp_domains_note: |
    The requirement text (JUP-07) mentions lite-api.jup.ag, terminal.jup.ag, and Meteora domains.
    Research found these are OUTDATED/INCORRECT:
    - lite-api.jup.ag is DEPRECATED (migrated to api.jup.ag with API key)
    - terminal.jup.ag is DEPRECATED (renamed to plugin.jup.ag)
    - Meteora has no API calls planned in this integration (out of scope)
    The ACTUAL domains needed are:
    - plugin.jup.ag (CDN script, styles, possible iframes)
    - api.jup.ag (Price API V3, server-side only but Helmet CSP covers it)
    - tokens.jup.ag (token list metadata)
    - cache.jup.ag (Plugin cached data)
---

<objective>
Update Content Security Policy for all Jupiter domains, add the Jupiter Plugin CDN script to index.html, and create a server-side Jupiter Price API V3 service that fetches live SHOT/SOL price — exposed to clients via a `getShotPrice` socket handler.

Purpose: CSP must be updated FIRST or the Plugin CDN and API calls will be blocked. The price service is server-side to protect the API key. This plan lays the foundation for Plans 02 and 03.
Output: Working CSP allowing Jupiter resources, Plugin CDN loaded, price service returning SHOT price via socket.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\johnk\SolShot-clean\.planning\ROADMAP.md
@C:\Users\johnk\SolShot-clean\.planning\STATE.md
@C:\Users\johnk\SolShot-clean\.planning\phases\09-jupiter-integration\09-RESEARCH.md
@C:\Users\johnk\SolShot-clean\client\public\index.html
@C:\Users\johnk\SolShot-clean\server\index.js
@C:\Users\johnk\SolShot-clean\server\socket-io\main.js
@C:\Users\johnk\SolShot-clean\server\.env.example
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CSP and add Jupiter Plugin CDN script</name>
  <files>
    client/public/index.html
    server/index.js
    server/.env.example
  </files>
  <action>
    **index.html CSP meta tag** — Update the existing `Content-Security-Policy` meta tag to add Jupiter domains.

    The ACTUAL domains required (per research — requirement text uses outdated names):
    - `plugin.jup.ag` — CDN script, styles, possible iframes (replaces deprecated terminal.jup.ag)
    - `api.jup.ag` — Price API V3 (replaces deprecated lite-api.jup.ag)
    - `tokens.jup.ag` — token list metadata used by Plugin
    - `cache.jup.ag` — Plugin cached data

    CSP directive updates:
    - `script-src`: Add `https://plugin.jup.ag` (for the Plugin CDN script)
    - `connect-src`: Add `https://api.jup.ag https://plugin.jup.ag https://tokens.jup.ag https://cache.jup.ag`
    - `style-src`: Add `https://plugin.jup.ag` (Plugin injects styles)
    - `frame-src`: Change from `'none'` to `https://plugin.jup.ag` (Plugin may use iframes)

    NOTE: lite-api.jup.ag and terminal.jup.ag are NOT needed — they are deprecated. Meteora domains are NOT needed — no Meteora API calls are planned. If the requirement text (JUP-07) references these, the requirement text is outdated. This plan uses the correct current domains.

    **index.html CDN script** — Add the Jupiter Plugin CDN script BEFORE the closing `</head>` tag (after the Telegram script):
    ```html
    <script src="https://plugin.jup.ag/plugin-v1.js" defer></script>
    ```
    Use `defer` so it doesn't block page render.

    **server/index.js Helmet CSP** — Update the Helmet `contentSecurityPolicy.directives` object to mirror the same additions:
    - `scriptSrc`: Add `"https://plugin.jup.ag"`
    - `connectSrc`: Add `"https://api.jup.ag"`, `"https://plugin.jup.ag"`, `"https://tokens.jup.ag"`, `"https://cache.jup.ag"`
    - `styleSrc`: Add `"https://plugin.jup.ag"`
    - `frameSrc`: Change from `["'none'"]` to `["https://plugin.jup.ag"]`

    CRITICAL: Do NOT modify any other part of server/index.js. Only touch the Helmet CSP directives object. Do not change middleware order, CORS config, rate limiting, or anything else.

    Also update `server/.env.example` to add:
    ```
    # Jupiter Price API (free Lite tier from portal.jup.ag)
    JUP_API_KEY=your-jupiter-api-key-here
    ```
  </action>
  <verify>
    1. `npx react-app-rewired build` completes in client/ directory (no CSP parse errors in HTML)
    2. Visually inspect index.html: Plugin CDN script tag present, CSP meta tag includes all 4 Jupiter domains (plugin.jup.ag, api.jup.ag, tokens.jup.ag, cache.jup.ag)
    3. Visually inspect server/index.js: Helmet CSP directives include all 4 Jupiter domains
    4. server/.env.example contains JUP_API_KEY entry
    5. Confirm NO references to lite-api.jup.ag, terminal.jup.ag, or Meteora domains in CSP (these are deprecated/out-of-scope)
  </verify>
  <done>
    CSP in both index.html and server Helmet allows script-src, connect-src, style-src, and frame-src for Jupiter Plugin and API domains. The exact domains are: plugin.jup.ag, api.jup.ag, tokens.jup.ag, cache.jup.ag. Plugin CDN script loads via defer. .env.example documents JUP_API_KEY.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create server-side Jupiter Price API service and socket handler</name>
  <files>
    server/services/jupiter-price.js
    server/socket-io/main.js
  </files>
  <action>
    **Create `server/services/jupiter-price.js`** (NEW FILE) — Server-side service that fetches SHOT/SOL price from Jupiter Price API V3.

    Requirements:
    - Use ES module syntax (`import`/`export`) — matches server convention
    - Read `JUP_API_KEY` from `process.env.JUP_API_KEY`
    - Use native `fetch` (Node 18+ built-in, no extra deps)
    - Endpoint: `https://api.jup.ag/price/v3?ids=4NnYBycLLo8acgbkLz2SyCXd3KU8jgHQLEmrVypi5VLd`
    - Include header: `x-api-key: ${JUP_API_KEY}`
    - Parse response: `data[MINT_ADDRESS]` object contains `{ usdPrice, priceChange24h }`
    - Cache price in memory with 30-second TTL (don't hit API on every socket request)
    - Export `getShotPrice()` — returns `{ usdPrice: number|null, priceChange24h: number|null, lastUpdated: number }` or `{ usdPrice: null, priceChange24h: null, lastUpdated: null }` on error
    - Export `startPricePolling(intervalMs = 30000)` — starts periodic fetch, stores result in module-level cache
    - Export `stopPricePolling()` — clears the interval
    - If `JUP_API_KEY` is not set, log a warning once and return null prices (don't crash)
    - If API returns error or token has no price data (pre-launch / no liquidity), return null prices gracefully
    - Log price fetches at debug level, errors at warn level

    ```javascript
    // Module structure:
    const SHOT_MINT = '4NnYBycLLo8acgbkLz2SyCXd3KU8jgHQLEmrVypi5VLd';
    let cachedPrice = { usdPrice: null, priceChange24h: null, lastUpdated: null };
    let pollInterval = null;

    async function fetchPrice() { /* fetch from api.jup.ag */ }
    export function getShotPrice() { return { ...cachedPrice }; }
    export function startPricePolling(intervalMs = 30000) { /* setInterval */ }
    export function stopPricePolling() { /* clearInterval */ }
    ```

    **Update `server/socket-io/main.js`** — Add a `getShotPrice` socket handler.

    At the TOP of main.js, add the import:
    ```javascript
    import { getShotPrice, startPricePolling } from '../services/jupiter-price.js';
    ```

    Near the other initialization calls at the start of the `mainsocket(io)` function, add:
    ```javascript
    startPricePolling(30000); // Poll price every 30s
    ```

    Inside the `io.on('connection', (socket) => { ... })` block, add a NEW handler (do NOT modify existing handlers):
    ```javascript
    socket.on('getShotPrice', () => {
        const price = getShotPrice();
        socket.emit('shotPrice', price);
    });
    ```

    CRITICAL: Do NOT modify any existing handlers in main.js. Only ADD the import at the top, the startPricePolling() call, and the new getShotPrice handler inside the connection block. Search for a good insertion point — after the last existing `socket.on(...)` block or in a logical grouping area.
  </action>
  <verify>
    1. `node -e "import('./server/services/jupiter-price.js').then(m => { console.log(typeof m.getShotPrice, typeof m.startPricePolling, typeof m.stopPricePolling); })"` from the project root prints `function function function`
    2. Server starts without errors: `cd server && node index.js` (briefly, then Ctrl+C) — no import errors
    3. grep main.js for `getShotPrice` — handler exists
    4. grep jupiter-price.js for `api.jup.ag/price/v3` — endpoint correct
  </verify>
  <done>
    Server-side jupiter-price.js service fetches SHOT price from Jupiter API V3 with 30s cache, handles missing API key and errors gracefully. Socket handler `getShotPrice` emits `shotPrice` events to requesting clients. Price polling starts on server boot.
  </done>
</task>

</tasks>

<verification>
1. Client build succeeds: `cd client && npx react-app-rewired build`
2. Server starts without import/syntax errors
3. CSP meta tag in index.html includes: plugin.jup.ag in script-src, api.jup.ag + plugin.jup.ag + tokens.jup.ag + cache.jup.ag in connect-src
4. Helmet CSP in server/index.js mirrors the same additions
5. NO references to deprecated domains (lite-api.jup.ag, terminal.jup.ag) or out-of-scope domains (Meteora)
6. jupiter-price.js exports getShotPrice, startPricePolling, stopPricePolling
7. main.js has getShotPrice socket handler
8. No existing main.js handlers were modified
</verification>

<success_criteria>
- CSP allows all Jupiter domains: plugin.jup.ag, api.jup.ag, tokens.jup.ag, cache.jup.ag (no console violations when Plugin CDN loads)
- Plugin CDN script tag present in index.html with defer attribute
- Server price service returns cached SHOT price (or null gracefully)
- getShotPrice socket handler wired in main.js
- Server boots cleanly with new imports
</success_criteria>

<output>
After completion, create `.planning/phases/09-jupiter-integration/09-01-SUMMARY.md`
</output>
