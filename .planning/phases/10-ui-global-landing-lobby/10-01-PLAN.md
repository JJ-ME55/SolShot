---
phase: 10-ui-global-landing-lobby
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ShotPriceTicker.js
  - client/src/components/TopBar.js
  - client/src/components/WalletDisplay.js
autonomous: true

must_haves:
  truths:
    - "SHOT price ticker is visible in the header bar on every screen that uses TopBar (Lobby, Shop, Armory, Prestige, Barracks, Battle)"
    - "Ticker shows live price formatted as 'SHOT $X.XXXX | +Y.Y%' with green/red color for positive/negative change"
    - "Ticker displays 'SHOT: N/A' when price data is unavailable (pre-launch, API down)"
    - "Ticker does not flash 'N/A' on initial load — it renders nothing until first server response"
    - "'What is a wallet?' help link appears below the wallet connect button on the MenuScreen (full WalletDisplay)"
    - "Help link does NOT appear in the compact TopBar wallet display"
  artifacts:
    - path: "client/src/components/ShotPriceTicker.js"
      provides: "Reusable SHOT price ticker component with socket subscription"
      exports: ["ShotPriceTicker"]
    - path: "client/src/components/TopBar.js"
      provides: "Three-column flex layout with ticker in center column"
      contains: "ShotPriceTicker"
    - path: "client/src/components/WalletDisplay.js"
      provides: "What is a wallet? help link for non-connected, non-compact mode"
      contains: "WHAT IS A WALLET"
  key_links:
    - from: "client/src/components/ShotPriceTicker.js"
      to: "server getShotPrice socket handler"
      via: "window.socket.emit('getShotPrice') + socket.on('shotPrice')"
      pattern: "socket\\.emit\\('getShotPrice'\\)"
    - from: "client/src/components/TopBar.js"
      to: "client/src/components/ShotPriceTicker.js"
      via: "import and render in center column"
      pattern: "import ShotPriceTicker"
---

<objective>
Create the ShotPriceTicker component, refactor TopBar to three-column flex layout with the ticker, and add a "What is a wallet?" help link to WalletDisplay.

Purpose: Every screen with a TopBar shows live SHOT price, and crypto-naive players get a help link on the landing page wallet section.
Output: ShotPriceTicker.js (new), TopBar.js (refactored layout), WalletDisplay.js (help link)
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-global-landing-lobby/10-RESEARCH.md
@client/src/components/TopBar.js
@client/src/components/WalletDisplay.js
@client/src/screens/WinScreen.js (lines 113-128 for shotPrice socket pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShotPriceTicker component and refactor TopBar layout</name>
  <files>client/src/components/ShotPriceTicker.js, client/src/components/TopBar.js</files>
  <action>
Create `client/src/components/ShotPriceTicker.js`:
- Extract the shotPrice socket pattern from WinScreen.js (useState for shotPrice, useEffect with window.socket subscription to 'shotPrice' event, emit 'getShotPrice' on mount, cleanup on unmount)
- Add a `ready` boolean state — render nothing (return null) until the first server response (prevents flash of "N/A")
- When ready but `shotPrice.usdPrice === null`: show "SHOT: N/A" in Share Tech Mono, 10px, color var(--sp), opacity 0.75, letterSpacing 1
- When ready with valid price: show "SHOT $X.XXXX" in same style, plus optional "| +Y.Y%" span colored #14F195 (green) for positive, #cc2200 (red) for negative change
- Accept an optional `style` prop that spreads onto the outer span for positioning overrides
- Guard with `if (!socket) return;` at top of useEffect

Refactor `client/src/components/TopBar.js`:
- Replace the current `position: relative` wrapper + `position: absolute` title layout with a three-column flex layout
- New wrapper: `display: 'flex', alignItems: 'center', width: '100%'`
- Left column: `flexShrink: 0, minWidth: 80` — contains back button (existing)
- Center column: `flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center', gap: 2` — contains title (existing) + ShotPriceTicker (new)
- Right column: `flexShrink: 0, minWidth: 80, display: 'flex', justifyContent: 'flex-end'` — contains WalletDisplay compact (existing)
- Remove `position: absolute`, `left: '50%'`, `transform: 'translateX(-50%)'` from the title style
- Import ShotPriceTicker and render it below the title in the center column
- Keep all existing props (title, onBack, showWallet) working identically

IMPORTANT: Do NOT use JSX template literals for the price text — use string concatenation (e.g., `'SHOT $' + price.toFixed(4)`) to avoid the pre-existing Collider.js ESLint issue that blocks builds.
  </action>
  <verify>
Run `cd client && npx react-app-rewired build 2>&1 | tail -20` — build completes with no errors.
Grep TopBar.js for `import ShotPriceTicker` — present.
Grep TopBar.js for `position: 'absolute'` — NOT present (removed).
Grep ShotPriceTicker.js for `getShotPrice` — present.
  </verify>
  <done>
ShotPriceTicker.js exists with socket subscription, ready-gate, N/A fallback, and price display. TopBar uses three-column flex with ticker in center. Build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add "What is a wallet?" help link to WalletDisplay</name>
  <files>client/src/components/WalletDisplay.js</files>
  <action>
In `client/src/components/WalletDisplay.js`, modify the `!connected` branch:
- After WalletMultiButton, add a conditional block that ONLY renders when `!compact`
- Render a small link: `<a href="https://solana.com/learn/blockchain-basics" target="_blank" rel="noopener noreferrer">` with text "WHAT IS A WALLET?"
- Style: fontFamily "'Share Tech Mono', monospace", fontSize 10, color 'var(--kh)', opacity 0.6, letterSpacing 1, textDecoration 'none', display 'block', textAlign 'center', marginTop 4
- The link opens in a new tab (target="_blank") — this is safe since the game runs in a browser tab, and CSP does not restrict `<a>` navigation

This ensures:
- MenuScreen (which renders `<WalletDisplay />` without compact) shows the help link below the wallet button
- TopBar (which renders `<WalletDisplay compact />`) does NOT show the help link
  </action>
  <verify>
Grep WalletDisplay.js for `WHAT IS A WALLET` — present.
Grep WalletDisplay.js for `!compact` — condition gates the help link.
Run `cd client && npx react-app-rewired build 2>&1 | tail -5` — build succeeds.
  </verify>
  <done>
"What is a wallet?" link visible below wallet connect button on MenuScreen only (not in compact TopBar mode). Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx react-app-rewired build` — full production build succeeds with zero errors
2. `client/src/components/ShotPriceTicker.js` exists and exports a React component
3. TopBar.js imports and renders ShotPriceTicker — no absolute positioning remains on title
4. WalletDisplay.js shows help link only when `!connected && !compact`
5. No new dependencies added (only React hooks + existing window.socket pattern)
</verification>

<success_criteria>
- ShotPriceTicker component renders SHOT price from server socket on all TopBar screens
- Ticker shows "N/A" for pre-launch state, nothing on initial load
- TopBar layout uses flex (not absolute positioning) — title and ticker centered
- "What is a wallet?" link visible on MenuScreen wallet section, hidden in TopBar
- Production build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-global-landing-lobby/10-01-SUMMARY.md`
</output>
