---
phase: 10-ui-global-landing-lobby
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/screens/MenuScreen.js
autonomous: true

must_haves:
  truths:
    - "Landing screen shows ecosystem partner badges for Solana, Jupiter, Meteora, and Claude — styled as CSS text badges with brand colors (no external images)"
    - "Three distinct CTAs are visible: 'PLAY FREE' (primary, navigates to lobby), 'CONNECT WALLET' (WalletMultiButton), and 'LEARN MORE' (anchor link or placeholder)"
    - "Landing copy includes a skill-based tagline ('SKILL, NOT LUCK' or similar), visible wager range ('0.1 TO 1.0 SOL'), and 'NO DOWNLOAD REQUIRED' message"
    - "Jupiter Mobile is highlighted as a wallet option with 'New to crypto? Start here.' messaging near the wallet section"
    - "Existing nav buttons (DEPLOY, ARMORY, PRESTIGE, BARRACKS) still work and remain visible"
  artifacts:
    - path: "client/src/screens/MenuScreen.js"
      provides: "Landing screen with ecosystem partners, CTAs, skill tagline, wager range, no-download messaging, Jupiter Mobile callout"
      contains: "PARTNERS"
  key_links:
    - from: "client/src/screens/MenuScreen.js"
      to: "lobby navigation"
      via: "navigate('lobby') on PLAY FREE click"
      pattern: "navigate.*lobby"
    - from: "client/src/screens/MenuScreen.js"
      to: "WalletDisplay / WalletMultiButton"
      via: "existing WalletDisplay component renders wallet connect"
      pattern: "WalletDisplay"
---

<objective>
Overhaul the MenuScreen landing page with ecosystem partner logos, three CTAs, marketing copy, and Jupiter Mobile callout.

Purpose: The landing page is the first thing new visitors see — it must communicate what SolShot is, show ecosystem credibility, and convert visitors into players.
Output: MenuScreen.js (modified with new sections)
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-ui-global-landing-lobby/10-RESEARCH.md
@client/src/screens/MenuScreen.js
@client/src/components/Button.js
@client/src/components/WalletDisplay.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Solana badge with ecosystem partners row</name>
  <files>client/src/screens/MenuScreen.js</files>
  <action>
In MenuScreen.js, replace the `solanaBadge` section (the `<div style={styles.solanaBadge}>` containing the Solana dot and "POWERED BY SOLANA" text) with a multi-partner ecosystem row:

Define a PARTNERS constant at the top of the file (outside the component):
```
const PARTNERS = [
  { name: 'SOLANA',   color: '#9945FF' },
  { name: 'JUPITER',  color: '#C7F284' },
  { name: 'METEORA',  color: '#00D4AA' },
  { name: 'CLAUDE',   color: '#D97706' },
];
```

Add new styles:
- `partnersRow`: display flex, gap 8, alignItems center, flexWrap wrap, justifyContent center, marginTop 10, marginBottom 20, zIndex 1
- `partnerBadge` (function taking color): fontFamily "'Share Tech Mono', monospace", fontSize 10, color: the partner color, letterSpacing 2, padding '2px 8px', border `1px solid ${color}44`, borderRadius 2, background `${color}0A`

Replace the solanaBadge JSX with:
```jsx
<div style={styles.partnersRow}>
  {PARTNERS.map((p) => (
    <span key={p.name} style={styles.partnerBadge(p.color)}>{p.name}</span>
  ))}
</div>
```

Remove the old solanaBadge, solanaDot, and solanaText style objects (dead code after replacement).

This uses CSS-only text badges — no external images, no CSP changes needed.
  </action>
  <verify>
Grep MenuScreen.js for `PARTNERS` — constant exists.
Grep MenuScreen.js for `SOLANA.*JUPITER.*METEORA.*CLAUDE` — all four partners present in PARTNERS array.
Grep MenuScreen.js for `solanaBadge` — should only appear in style definition removal or not at all. The old JSX using solanaBadge should be replaced.
  </verify>
  <done>
Ecosystem partner badges row with Solana, Jupiter, Meteora, Claude replaces old single "POWERED BY SOLANA" badge with brand-colored CSS text badges.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add three CTAs, landing copy, and Jupiter Mobile callout</name>
  <files>client/src/screens/MenuScreen.js</files>
  <action>
Modify MenuScreen.js to add the remaining landing page requirements. All changes are within the existing component — no new files.

**1. Update tagline and add copy (UI-05):**
Replace the existing tagline text `'ARTILLERY COMBAT ON SOLANA'` with `'SKILL, NOT LUCK'`.
Add a second line below the tagline: `'WAGER 0.1 — 1.0 SOL | NO DOWNLOAD REQUIRED'` styled similarly but slightly smaller (fontSize 11, opacity 0.5, letterSpacing 2).

**2. Three CTAs (UI-04):**
The existing nav button "DEPLOY" already navigates to the lobby — rename its label to `'PLAY FREE'` and keep it as the primary CTA (variant: 'primary'). This is the most prominent CTA.

"CONNECT WALLET" is already served by the WalletDisplay component at the bottom of the screen (renders WalletMultiButton when !connected). No duplicate button needed — the wallet section IS the Connect Wallet CTA.

Add a "LEARN MORE" text link below the wallet section. Use an `<a>` tag with `href="#"` as a placeholder URL (the litepaper URL can be updated later). Style: fontFamily "'Share Tech Mono', monospace", fontSize 11, color 'var(--kh)', opacity 0.5, letterSpacing 2, textDecoration 'none', textTransform 'uppercase', zIndex 1. On hover, opacity 0.8.

**3. Jupiter Mobile callout (UI-06):**
Add a small callout text directly below the WalletDisplay section (before the Learn More link):
Text: `'NEW TO CRYPTO? USE JUPITER MOBILE'`
Style: fontFamily "'Share Tech Mono', monospace", fontSize 10, color '#C7F284' (Jupiter green), opacity 0.6, letterSpacing 1, textAlign 'center', marginTop 4, zIndex 1

**Summary of layout changes (top to bottom):**
1. Logo (unchanged)
2. Tagline: "SKILL, NOT LUCK" (updated)
3. Sub-tagline: "WAGER 0.1 — 1.0 SOL | NO DOWNLOAD REQUIRED" (new)
4. Ecosystem partners row (from Task 1)
5. Nav buttons: PLAY FREE, ARMORY, PRESTIGE, BARRACKS (DEPLOY renamed to PLAY FREE)
6. Telegram badge (unchanged, conditional)
7. Wallet section (unchanged)
8. Jupiter Mobile callout (new)
9. Learn More link (new)
10. Version tag (unchanged)
11. Responsible Gaming (unchanged)

Keep existing nav items (ARMORY, PRESTIGE, BARRACKS) — the CTAs supplement, not replace.
  </action>
  <verify>
Grep MenuScreen.js for `PLAY FREE` — present (renamed from DEPLOY).
Grep MenuScreen.js for `SKILL, NOT LUCK` — present as tagline.
Grep MenuScreen.js for `NO DOWNLOAD` — present in sub-tagline.
Grep MenuScreen.js for `LEARN MORE` — present as link.
Grep MenuScreen.js for `JUPITER MOBILE` — present as callout.
Grep MenuScreen.js for `WAGER 0.1` — present showing wager range.
Run `cd client && npx react-app-rewired build 2>&1 | tail -5` — build succeeds.
  </verify>
  <done>
MenuScreen shows "SKILL, NOT LUCK" tagline, wager range, "NO DOWNLOAD REQUIRED", "PLAY FREE" primary CTA, "LEARN MORE" link, Jupiter Mobile callout. All existing navigation preserved. Build succeeds.
  </done>
</task>

</tasks>

<verification>
1. `cd client && npx react-app-rewired build` — production build succeeds
2. MenuScreen.js has PARTNERS array with 4 ecosystem partners
3. Tagline reads "SKILL, NOT LUCK" (not old "ARTILLERY COMBAT ON SOLANA")
4. Sub-tagline shows wager range and "NO DOWNLOAD REQUIRED"
5. "DEPLOY" renamed to "PLAY FREE" — same navigation target
6. Jupiter Mobile callout present below wallet section
7. "LEARN MORE" link present
8. Existing nav buttons (ARMORY, PRESTIGE, BARRACKS) still functional
</verification>

<success_criteria>
- Ecosystem partner badges show Solana, Jupiter, Meteora, Claude with brand colors
- Three CTAs visible: PLAY FREE (primary nav), Connect Wallet (WalletDisplay), Learn More (link)
- Landing copy includes skill tagline, wager range, no-download message
- Jupiter Mobile highlighted with "NEW TO CRYPTO?" callout
- All existing navigation works unchanged
- Production build passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-global-landing-lobby/10-02-SUMMARY.md`
</output>
