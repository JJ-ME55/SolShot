---
phase: 11-post-match-stats-pipeline
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - client/src/screens/WinScreen.js
  - client/src/screens/LoseScreen.js
  - client/src/screens/LobbyScreen.js
autonomous: true

must_haves:
  truths:
    - "WinScreen has three tabs (Result, Progress, Action) — Result shows current rewards, Progress shows SHOT milestones earned and prestige progress, Action shows JupiterSwap and buttons"
    - "LoseScreen has the same three-tab structure with loss-appropriate content"
    - "Post-match Progress tab shows itemized SHOT milestones earned this match (from matchEnd.earnedMilestones) with id/label/reward"
    - "Post-match Progress tab shows prestige tier progress bar (current SHOT balance toward next tier burn cost) from matchEnd.prestigeInfo"
    - "LobbyScreen shows a one-time escrow explainer modal when a player first selects a wager amount > 0, tracked via localStorage"
  artifacts:
    - path: "client/src/screens/WinScreen.js"
      provides: "Tabbed post-match UI with milestones and prestige progress"
      contains: "earnedMilestones"
    - path: "client/src/screens/LoseScreen.js"
      provides: "Tabbed post-match UI matching WinScreen structure"
      contains: "earnedMilestones"
    - path: "client/src/screens/LobbyScreen.js"
      provides: "Escrow explainer modal on first wager selection"
      contains: "solshot_escrow_seen"
  key_links:
    - from: "WinScreen.js (Progress tab)"
      to: "screenData.earnedMilestones[myId]"
      via: "Array.map rendering milestone items"
      pattern: "earnedMilestones"
    - from: "WinScreen.js (Progress tab)"
      to: "screenData.prestigeInfo[myId]"
      via: "Prestige progress bar calculation"
      pattern: "prestigeInfo"
    - from: "LobbyScreen.js"
      to: "localStorage solshot_escrow_seen"
      via: "One-time modal gate"
      pattern: "solshot_escrow_seen"
---

<objective>
Add tabbed navigation to WinScreen and LoseScreen (Result | Progress | Action), display SHOT milestones earned and prestige tier progress on the Progress tab, and add a one-time escrow explainer modal to LobbyScreen when a player first selects a wager.

Purpose: Players need to understand what they earned (milestones), how close they are to the next prestige tier, and (for first-time wager players) how the escrow system protects their funds.
Output: Tabbed WinScreen/LoseScreen with milestone and prestige data, escrow explainer on LobbyScreen.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-post-match-stats-pipeline/11-01-SUMMARY.md
@client/src/screens/WinScreen.js
@client/src/screens/LoseScreen.js
@client/src/screens/LobbyScreen.js
@client/src/data/tiers.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add tabbed layout to WinScreen with Result/Progress/Action tabs and milestone/prestige display</name>
  <files>client/src/screens/WinScreen.js</files>
  <action>
Refactor WinScreen to use a three-tab layout. Add local state `const [activeTab, setActiveTab] = useState('result');`.

**Tab navigation styles** — Add to the `s` style object:
- `tabNav`: flex row, centered, gap 0, maxWidth 400, width '100%', borderBottom: '1px solid var(--ol)'
- `tab`: (active) => padding '8px 20px', cursor 'pointer', fontFamily "'Share Tech Mono', monospace", fontSize 11, letterSpacing 2, color active ? 'var(--am)' : 'var(--kh)', borderBottom active ? '2px solid var(--am)' : '2px solid transparent', opacity active ? 1 : 0.5, background 'none', border 'none' (except bottom), textTransform 'uppercase'

**Tab content panels:**

**Result tab** (default) — Move the existing reward cards (SOL earned, SHOT earned, gold), stats grid (rounds won, dmg dealt, kills), and settlement TX display into this tab. This is the current content, just wrapped in a conditional: `{activeTab === 'result' && ( ... )}`.

**Progress tab** — New content:
1. **SHOT Milestones Earned** — Read `screenData.earnedMilestones` (object keyed by playerId). Extract `const myMilestones = (screenData?.earnedMilestones && myId) ? (screenData.earnedMilestones[myId] || []) : [];`. If milestones exist, render a receipt-style list:
   - Section label: "MILESTONES EARNED" (Share Tech Mono, 11px, var(--am), letterSpacing 2)
   - For each milestone: row with label on left, "+" + reward + " SHOT" on right
   - Style: background rgba(42,51,31,0.2), border 1px solid var(--ol), borderRadius 4, padding 8px 12px, margin 3px 0
   - If no milestones: show "No new milestones this match" in dim text

2. **Prestige Progress** — Read `screenData.prestigeInfo` (object keyed by playerId). Extract `const myPrestige = (screenData?.prestigeInfo && myId) ? (screenData.prestigeInfo[myId] || null) : null;`.
   - Show current tier name and color (e.g., "BRONZE" in tier color)
   - Show SHOT balance: myPrestige.balance
   - If myPrestige.nextTier exists:
     - Progress bar: width = (myPrestige.balance / myPrestige.nextTier.burnCost) * 100, clamped to 100
     - Label: myPrestige.balance + " / " + myPrestige.nextTier.burnCost + " SHOT to " + myPrestige.nextTier.name
     - Bar style: track background var(--od), fill gradient var(--ru) to var(--rg), height 6px, borderRadius 3
   - If at max tier (Diamond): show "MAX TIER REACHED" in gold
   - Show matches played: myPrestige.matchesPlayed

**Action tab** — Move the existing JupiterSwap CTA section, rematch/lobby/menu buttons, and waiting text into this tab. Also move the opponent left modal here (it should show regardless of tab, so keep the Modal outside tab conditionals).

**IMPORTANT coding patterns:**
- Use string concatenation (not template literals) in JSX — avoids Collider.js ESLint webpack worker bug
- Import nothing new — all data comes from screenData props
- Keep existing useSocket handlers, shotPrice fetch, and callback hooks OUTSIDE the tab conditionals (they must always be active)
- The hero banner image stays above the tabs (always visible)
  </action>
  <verify>
Grep WinScreen.js for `activeTab` — should appear.
Grep WinScreen.js for `earnedMilestones` — should appear.
Grep WinScreen.js for `prestigeInfo` — should appear.
Grep WinScreen.js for `'result'` and `'progress'` and `'action'` — all three tab names should appear.
Verify no template literals in JSX: `grep -n '\`' client/src/screens/WinScreen.js` should return no JSX template literal usage (server-side string operations are OK).
  </verify>
  <done>WinScreen has three tabs. Result tab shows rewards and match stats. Progress tab shows SHOT milestones earned and prestige tier progress bar. Action tab shows JupiterSwap and navigation buttons.</done>
</task>

<task type="auto">
  <name>Task 2: Mirror tabbed layout on LoseScreen and add escrow explainer to LobbyScreen</name>
  <files>client/src/screens/LoseScreen.js, client/src/screens/LobbyScreen.js</files>
  <action>
**LoseScreen.js** — Apply the same three-tab structure as WinScreen:

- Add `const [activeTab, setActiveTab] = useState('result');` state
- Add identical tab nav styles and rendering
- **Result tab**: Move existing loss card (SOL wagered), stats grid (rounds won, dmg dealt, gold), and any existing content
- **Progress tab**: Same milestone and prestige display as WinScreen (read from screenData.earnedMilestones and screenData.prestigeInfo). Even losers earn SHOT from milestones.
- **Action tab**: Move JupiterSwap CTA (GET SHOT FOR PRESTIGE), rematch/lobby/menu buttons. CTA label should remain "GET SHOT FOR PRESTIGE UPGRADES" (different from WinScreen's "CONVERT WINNINGS TO SHOT").
- Keep hero banner above tabs, opponent left modal outside tab conditionals
- Same coding patterns as WinScreen (string concatenation, no template literals in JSX)

**LobbyScreen.js** — Add a one-time escrow explainer modal:

1. Add state: `const [showEscrow, setShowEscrow] = useState(false);`

2. Create a handler that fires when the player selects a wager amount > 0 for the first time. Find the wager selection handler (where wager buttons are clicked). BEFORE or AFTER the wager is set, check:
   ```js
   if (amount > 0 && !localStorage.getItem('solshot_escrow_seen')) {
       setShowEscrow(true);
   }
   ```

3. Render the escrow explainer using the existing `Modal` component:
   ```jsx
   {showEscrow && (
       <Modal
           title="HOW WAGERING WORKS"
           message="Your SOL is held by a smart contract (escrow) during the match. The winner receives 90% of the pot. Neither player nor SolShot can access funds during the match. If your opponent disconnects, you get a full refund."
           buttons={[{
               label: 'GOT IT',
               variant: 'primary',
               onClick: () => {
                   localStorage.setItem('solshot_escrow_seen', 'true');
                   setShowEscrow(false);
               }
           }]}
           onClose={() => {
               localStorage.setItem('solshot_escrow_seen', 'true');
               setShowEscrow(false);
           }}
       />
   )}
   ```

4. The modal does NOT block wager selection — it's informational. The wager is still set; the modal just educates.

IMPORTANT: Do NOT modify the existing wager selection logic — only ADD the localStorage check and modal render. Search for the wager handler carefully in LobbyScreen.js (it may be a click handler on wager amount buttons).
  </action>
  <verify>
Grep LoseScreen.js for `activeTab` — should appear.
Grep LoseScreen.js for `earnedMilestones` — should appear.
Grep LoseScreen.js for `prestigeInfo` — should appear.
Grep LobbyScreen.js for `solshot_escrow_seen` — should appear.
Grep LobbyScreen.js for `HOW WAGERING WORKS` — should appear.
Client builds without errors: `cd client && npx react-scripts build 2>&1 | tail -5` (should show "Compiled successfully" or similar).
  </verify>
  <done>LoseScreen has three tabs matching WinScreen structure. LobbyScreen shows one-time escrow explainer modal on first wager selection, persisted via localStorage.</done>
</task>

</tasks>

<verification>
1. WinScreen renders three tabs (Result, Progress, Action) with active tab highlighting
2. WinScreen Progress tab reads and displays earnedMilestones from screenData
3. WinScreen Progress tab reads and displays prestigeInfo with progress bar toward next tier
4. LoseScreen has matching three-tab structure
5. LobbyScreen shows escrow explainer modal once (first wager > 0), dismissed with GOT IT
6. localStorage key `solshot_escrow_seen` prevents re-showing the modal
7. All existing functionality (rematch, lobby nav, JupiterSwap, opponent left modal) still works
8. Client builds without errors
</verification>

<success_criteria>
- WinScreen and LoseScreen both have Result/Progress/Action tabs
- Progress tab shows SHOT milestones earned in this match and prestige tier progress
- LobbyScreen shows a one-time escrow explainer before first wager
- No regressions in existing post-match flow (rematch, nav, swap)
- Client compiles successfully
</success_criteria>

<output>
After completion, create `.planning/phases/11-post-match-stats-pipeline/11-02-SUMMARY.md`
</output>
