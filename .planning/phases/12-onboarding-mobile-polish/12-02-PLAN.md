---
phase: 12-onboarding-mobile-polish
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - client/src/screens/LobbyScreen.js
  - client/src/components/FAQ.js
  - client/src/App.js
autonomous: true

must_haves:
  truths:
    - "A new player reaches their first practice match in under 60 seconds from the landing page"
    - "An FAQ page is accessible from every screen via a single tap"
  artifacts:
    - path: "client/src/screens/LobbyScreen.js"
      provides: "Default mode set to practice for new players"
      contains: "practice"
    - path: "client/src/components/FAQ.js"
      provides: "FAQ page component with game help content"
      exports: ["default"]
      min_lines: 50
  key_links:
    - from: "client/src/screens/LobbyScreen.js"
      to: "practice mode"
      via: "default state initialization"
      pattern: "useState.*practice"
    - from: "client/src/components/FAQ.js"
      to: "App.js"
      via: "global FAQ button renders FAQ modal"
      pattern: "FAQ"
    - from: "client/src/App.js"
      to: "client/src/components/FAQ.js"
      via: "import and render FAQ button + modal"
      pattern: "import.*FAQ"
---

<objective>
Onboarding flow optimization and FAQ help system.

Purpose: New players must reach their first practice match in under 60 seconds (ONB-01). An FAQ provides help from any screen with a single tap (ONB-04).

Output: LobbyScreen defaults to practice mode, FAQ component with global access button in App.js.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-onboarding-mobile-polish/12-01-SUMMARY.md

Key codebase knowledge (from project memory):
- LobbyScreen.js has mode tabs (Practice/Quick Match/Duel/High Roller) — research says default MUST be 'practice'
- MATCH_MODES defined in both server/services/solana.js and client/src/screens/LobbyScreen.js — keep in sync
- Client uses useCallback extensively — check dependency arrays when modifying callbacks
- App.js already has `import './utils/haptic'` added by Plan 01 — add FAQ imports AFTER that line
- Layout.js already has landscape/dApp overlays from Plan 01 — do NOT duplicate those
- Use string concatenation (not template literals) in JSX per project convention (Collider.js ESLint bug)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Default LobbyScreen to practice mode for 60-second onboarding</name>
  <files>client/src/screens/LobbyScreen.js</files>
  <action>
Change LobbyScreen's default match mode to practice so new players land directly on the Practice tab.

1. Find the mode state initialization in LobbyScreen.js — it likely uses useState with one of the MATCH_MODES values (e.g., `useState('quickmatch')` or `useState(MATCH_MODES.QUICK)`)
2. Change the default/initial value to `'practice'` (or whatever the practice mode constant is — check MATCH_MODES definition in the same file)
3. This ensures:
   - New players land directly on Practice tab
   - No extra clicks needed to start a practice match
   - The 60-second flow: Landing page CTA -> connect wallet -> lobby (already on Practice) -> click Play -> in match

4. Do NOT change:
   - Server-side mode validation
   - The MATCH_MODES definition
   - Any other LobbyScreen behavior
   - Tab rendering order or styling

5. Verify the mode tab UI still highlights Practice as the selected/active tab on initial render by checking how the tab active state is derived from the mode state.

This is a ONE-LINE change — only the default value of the mode useState hook.
  </action>
  <verify>
    - LobbyScreen.js initializes mode state to 'practice' (or equivalent practice constant)
    - Practice tab appears selected/active on initial load
    - Other mode tabs still work when clicked
    - `npm run build` in client/ succeeds
  </verify>
  <done>LobbyScreen defaults to practice mode on load. New players see the Practice tab immediately without extra clicks, enabling the 60-second landing-to-match flow.</done>
</task>

<task type="auto">
  <name>Task 2: Create FAQ component and global access button</name>
  <files>client/src/components/FAQ.js, client/src/App.js</files>
  <action>
Create a FAQ help system accessible from every screen with one tap.

**A. FAQ Component — client/src/components/FAQ.js:**

1. Create a full-screen modal/overlay component with accordion-style FAQ sections
2. Props: `isOpen` (boolean), `onClose` (callback)
3. FAQ sections (each collapsible — click header to expand/collapse):

   - **How do I play?**
     "SolShot is a turn-based artillery game. Adjust your angle and power, then fire! Take turns with your opponent until one tank is destroyed. Each match, you pick 10 weapons from a random selection."

   - **How do weapons work?**
     "You get 20 weapons to choose from at the start of each match — pick your best 10. Each weapon has different damage, blast radius, and special effects. Unlock 5 prestige weapons by burning SHOT tokens."

   - **What is SHOT?**
     "SHOT is the SolShot game token. You earn it from playing matches. Burn SHOT to unlock prestige tiers (Bronze through Diamond), each granting access to more powerful exclusive weapons."

   - **What are prestige tiers?**
     "There are 5 prestige tiers: Bronze, Silver, Gold, Platinum, and Diamond. Each tier unlocks exclusive weapons and cosmetics. Advance by burning SHOT tokens on the Prestige screen."

   - **How does wagering work?**
     "In Duel and High Roller modes, you wager SOL against your opponent. Wagers are held in a trustless on-chain escrow. Winners receive 90% of the pot, with 7% to treasury and 3% to operations."

   - **Which wallets work?**
     "Any Solana wallet that supports Wallet Adapter — Phantom, Solflare, Backpack, and more. Connect your wallet to play wagered matches and earn SHOT."

   - **Best experience on mobile?**
     "Use Chrome or Safari in landscape mode for the best experience. Wallet dApp browsers are locked to portrait — open solshot.gg directly in your mobile browser instead."

4. Styling:
   - Full-screen overlay: fixed position, inset 0, z-index 9500 (above FAQ button at 9000, below Layout.js overlays at 9999)
   - Dark backdrop (rgba(0,0,0,0.85))
   - Content area: centered, max-width 600px, max-height 80vh, overflow-y auto, padding 24px
   - Close button: "X" in top-right corner of content area
   - Section headers: clickable, with expand/collapse indicator (+/-)
   - Use inline styles matching existing project patterns
   - Close on Escape key (add useEffect with keydown listener)
   - Close on backdrop click (onClick on backdrop, stopPropagation on content)

5. Accordion state: Use a single `openSection` useState (null or section index). Clicking a header toggles it. Only one section open at a time.

**B. FAQ Button + State in App.js:**

1. In App.js, import FAQ component: `import FAQ from './components/FAQ'`
2. Add state: `const [faqOpen, setFaqOpen] = useState(false)`
3. Render the FAQ button as a fixed-position element:
   - Position: `position: fixed, bottom: 12px, right: 12px, zIndex: 9000`
   - Appearance: 40px circle, dark background with slight transparency, white "?" text, bold
   - Border: 2px solid with a subtle color (match project's accent color if identifiable, otherwise white/gray)
   - Cursor: pointer
   - onClick: `() => setFaqOpen(true)`
4. Render the FAQ modal: `<FAQ isOpen={faqOpen} onClose={() => setFaqOpen(false)} />`
5. Place button and FAQ modal AFTER the main app routes/content but still inside the top-level wrapper

Note: App.js already has `import './utils/haptic'` from Plan 01. Add the FAQ import on a separate line after it.

Use string concatenation (not template literals) in any JSX.
  </action>
  <verify>
    - FAQ.js component exists with 7 FAQ sections
    - Each section is collapsible (accordion pattern)
    - App.js imports FAQ and renders the "?" button + FAQ modal
    - FAQ button is positioned bottom-right (12px, 12px) with z-index 9000
    - FAQ modal opens on button click, closes on X/Escape/backdrop click
    - Only one FAQ section open at a time
    - `npm run build` in client/ succeeds
  </verify>
  <done>FAQ help system accessible from every screen via a fixed "?" button at bottom-right. Opens a full-screen overlay with 7 collapsible FAQ sections covering gameplay, SHOT, prestige, wagering, wallets, and mobile tips.</done>
</task>

</tasks>

<verification>
- `npm run build` in client/ completes without errors
- LobbyScreen defaults to practice mode on load
- FAQ "?" button visible on every screen at bottom-right
- FAQ modal opens with 7 sections, each collapsible
- FAQ closes via X button, Escape key, or backdrop click
- No interference with Layout.js overlays from Plan 01 (z-index layering correct)
</verification>

<success_criteria>
- ONB-01: LobbyScreen defaults to practice — new players reach first match faster
- ONB-04: FAQ accessible from every screen via bottom-right "?" button with comprehensive game help
- App.js changes are additive on top of Plan 01's haptic import
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-mobile-polish/12-02-SUMMARY.md`
</output>
