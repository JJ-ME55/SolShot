---
phase: 12-onboarding-mobile-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/components/ShotExplainer.js
  - client/src/components/PrestigeIntro.js
  - client/src/components/TelegramShare.js
  - client/src/screens/WinScreen.js
  - client/src/screens/LoseScreen.js
autonomous: true

must_haves:
  truths:
    - "When a player first earns SHOT tokens, a modal explains what SHOT is and what it can be used for"
    - "Prestige system is introduced contextually after gameplay, not front-loaded at landing"
    - "A Telegram share button appears on the post-match screen with pre-filled match result text"
  artifacts:
    - path: "client/src/components/ShotExplainer.js"
      provides: "SHOT token explainer modal triggered on first earn"
      exports: ["default"]
      min_lines: 30
    - path: "client/src/components/PrestigeIntro.js"
      provides: "Prestige system contextual introduction nudge"
      exports: ["default"]
      min_lines: 25
    - path: "client/src/components/TelegramShare.js"
      provides: "Telegram share button component"
      exports: ["default"]
      min_lines: 15
    - path: "client/src/screens/WinScreen.js"
      provides: "Integrates ShotExplainer, PrestigeIntro, and TelegramShare"
      contains: "ShotExplainer"
    - path: "client/src/screens/LoseScreen.js"
      provides: "Integrates ShotExplainer, PrestigeIntro, and TelegramShare"
      contains: "TelegramShare"
  key_links:
    - from: "client/src/components/ShotExplainer.js"
      to: "localStorage"
      via: "solshot_shot_explained flag for one-time display"
      pattern: "solshot_shot_explained"
    - from: "client/src/components/PrestigeIntro.js"
      to: "localStorage"
      via: "solshot_prestige_intro_seen flag and solshot_matches_played counter"
      pattern: "solshot_prestige_intro_seen"
    - from: "client/src/components/TelegramShare.js"
      to: "https://t.me/share/url"
      via: "window.open with encoded URL and text"
      pattern: "t\\.me/share"
    - from: "client/src/screens/WinScreen.js"
      to: "client/src/components/ShotExplainer.js"
      via: "import and conditional render as overlay"
      pattern: "import.*ShotExplainer"
---

<objective>
Post-match enhancements: contextual SHOT/prestige education and Telegram sharing.

Purpose: Players learn about SHOT tokens and prestige naturally through gameplay (ONB-02, ONB-03), not through front-loaded tutorials. Telegram sharing enables organic social growth (MOB-02). All three features integrate into the existing post-match WinScreen/LoseScreen.

Output: ShotExplainer modal, PrestigeIntro nudge, TelegramShare button — all wired into post-match screens.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

Key codebase knowledge (from project memory):
- WinScreen/LoseScreen have tab structure: Result / Progress / Action (from Phase 11)
- Existing localStorage pattern: 'solshot_escrow_seen' for one-time escrow explainer — follow same pattern
- X/Twitter share already exists in post-match Action tab (Phase 11): ShareCard forwardRef, clipboard copy, window.open tweet URL
- Telegram share URL: https://t.me/share/url?url=X&text=Y (identical pattern to X share)
- Prestige tiers: client/src/data/tiers.js (Bronze to Diamond, 5 tiers)
- Prestige badge assets exist in client/public/assets/
- Use string concatenation (not template literals) in JSX per project convention (Collider.js ESLint bug)
- Post-match screens receive match result data as props or from context
- Client uses useCallback extensively — check dependency arrays when modifying callbacks
- Navigation uses prop-drilled `navigate` callback (not React Router). Pass `onNavigatePrestige={() => navigate('prestige')}` to PrestigeIntro.
- Post-match screens have access to `roundWins` object keyed by socket ID. Use `roundWins[myId] || 0` for player score and the other key's value for opponent score. Scores represent rounds won (e.g., "3-1").
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ShotExplainer modal and PrestigeIntro nudge components</name>
  <files>client/src/components/ShotExplainer.js, client/src/components/PrestigeIntro.js</files>
  <action>
Create two education components that introduce SHOT and prestige contextually.

**A. ShotExplainer — client/src/components/ShotExplainer.js:**

A one-time modal that explains SHOT tokens when a player first earns them.

1. Props: `isOpen` (boolean), `onClose` (callback)
2. Content:
   - Header: "You earned SHOT!" (celebratory tone)
   - Body — three concise points:
     - "SHOT is the SolShot game token. You earn it by playing matches."
     - "Burn SHOT to unlock prestige tiers — Bronze, Silver, Gold, Platinum, Diamond."
     - "Each tier unlocks exclusive weapons with unique abilities."
   - CTA button: "Got it!" (calls onClose)
3. Styling:
   - Centered modal with dark backdrop (rgba(0,0,0,0.8))
   - z-index: 9800 (above post-match content, below Layout.js overlays at 9999)
   - Content box: max-width 400px, padding 24px, rounded corners, dark background matching project theme
   - Header: larger font, maybe gold/yellow accent color
   - Button: full-width, prominent, project's primary button style
   - Use inline styles matching existing modal patterns in the project

**B. PrestigeIntro — client/src/components/PrestigeIntro.js:**

A contextual nudge (NOT a full modal) that introduces prestige after sufficient gameplay.

1. Props: `currentTier` (string or null — player's current prestige tier), `shotBalance` (number), `matchesPlayed` (number), `onNavigatePrestige` (callback to go to PrestigeScreen)
2. Internal state:
   - Check localStorage `'solshot_prestige_intro_seen'` — if set, render nothing
   - Check conditions: `matchesPlayed >= 3` AND `currentTier` is null/base AND `shotBalance > 0`
   - If any condition fails, render nothing
3. Content (when visible):
   - Slim card/banner: "Ready to level up? Burn SHOT to unlock prestige tiers and exclusive weapons!"
   - Show the Bronze tier badge icon (first tier — reference from client/public/assets/ prestige badges)
   - Two small buttons: "Learn More" and "Later"
4. Actions:
   - "Learn More": set `localStorage.setItem('solshot_prestige_intro_seen', 'true')`, call `onNavigatePrestige()`
   - "Later": set `localStorage.setItem('solshot_prestige_intro_seen', 'true')`, hide component
5. Styling:
   - NOT a full-screen modal — a card/banner that fits WITHIN the Progress tab content
   - Subtle background, small padding, compact layout
   - Should feel like a helpful suggestion, not an interruption
   - Use inline styles

Use string concatenation (not template literals) in JSX.
  </action>
  <verify>
    - ShotExplainer.js exists with modal content, isOpen/onClose props
    - PrestigeIntro.js exists with conditional rendering logic
    - PrestigeIntro checks localStorage, matchesPlayed, currentTier, shotBalance
    - PrestigeIntro accepts onNavigatePrestige callback prop
    - Both components use inline styles matching project patterns
    - Neither component imports external CSS or UI libraries
    - `npm run build` in client/ succeeds (may need to test after Task 2 integration)
  </verify>
  <done>ShotExplainer modal ready for one-time SHOT education. PrestigeIntro nudge ready for contextual prestige introduction with onNavigatePrestige callback. Both are self-contained components with localStorage-based show-once logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create TelegramShare component</name>
  <files>client/src/components/TelegramShare.js</files>
  <action>
Create a Telegram share button that mirrors the existing X/Twitter share pattern.

1. Create `client/src/components/TelegramShare.js`
2. Props:
   - `isWinner` (boolean) — determines share text tone
   - `playerScore` (number) — player's rounds won
   - `opponentScore` (number) — opponent's rounds won
   - `shareUrl` (string, optional) — defaults to 'https://solshot.gg'

3. Share text construction (use string concatenation, NOT template literals):
   - Winner text: "I just won " + playerScore + "-" + opponentScore + " on SolShot! Artillery combat on Solana. Play at solshot.gg"
   - Loser text: "Just played an intense " + opponentScore + "-" + playerScore + " match on SolShot! Artillery combat on Solana. Play at solshot.gg"
   - Scores represent rounds won (e.g., "3-1"). Keep under 200 characters for clean Telegram preview.

4. Build Telegram share URL:
   - `'https://t.me/share/url?url=' + encodeURIComponent(shareUrl) + '&text=' + encodeURIComponent(shareText)`

5. Render:
   - A button matching the existing X/Twitter share button styling
   - Telegram brand color: #0088cc
   - Inline SVG for Telegram icon (paper plane):
     ```
     <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor">
       <path d="M11.944 0A12 12 0 0 0 0 12a12 12 0 0 0 12 12 12 12 0 0 0 12-12A12 12 0 0 0 12 0a12 12 0 0 0-.056 0zm4.962 7.224c.1-.002.321.023.465.14a.506.506 0 0 1 .171.325c.016.093.036.306.02.472-.18 1.898-.962 6.502-1.36 8.627-.168.9-.499 1.201-.82 1.23-.696.065-1.225-.46-1.9-.902-1.056-.693-1.653-1.124-2.678-1.8-1.185-.78-.417-1.21.258-1.91.177-.184 3.247-2.977 3.307-3.23.007-.032.014-.15-.056-.212s-.174-.041-.249-.024c-.106.024-1.793 1.14-5.061 3.345-.48.33-.913.49-1.302.48-.428-.008-1.252-.241-1.865-.44-.752-.245-1.349-.374-1.297-.789.027-.216.325-.437.893-.663 3.498-1.524 5.83-2.529 6.998-3.014 3.332-1.386 4.025-1.627 4.476-1.635z"/>
     </svg>
     ```
   - Button text: "Telegram" (short, matching X button which likely just says "X" or has icon + short text)
   - onClick: `window.open(telegramUrl, '_blank', 'noopener,noreferrer')`

6. Style: Match dimensions, padding, font-size, border-radius of the existing share button in the Action tab. If the X share button has specific classes or inline style patterns, replicate them. Use inline styles.
  </action>
  <verify>
    - TelegramShare.js exists with branded Telegram button
    - Uses https://t.me/share/url format with encoded params
    - Has inline SVG Telegram icon
    - Accepts isWinner, playerScore, opponentScore props
    - Share text differs for winner vs loser
    - Share text uses "rounds won" framing (e.g., "3-1")
    - Uses string concatenation (no template literals in JSX)
    - `npm run build` in client/ succeeds
  </verify>
  <done>TelegramShare component renders a Telegram-branded share button. Opens Telegram share dialog with contextual match result text (rounds won) and solshot.gg link.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate all three components into WinScreen and LoseScreen</name>
  <files>client/src/screens/WinScreen.js, client/src/screens/LoseScreen.js</files>
  <action>
Wire ShotExplainer, PrestigeIntro, and TelegramShare into both post-match screens.

**A. WinScreen.js integration:**

1. Import all three components at the top:
   - `import ShotExplainer from '../components/ShotExplainer'`
   - `import PrestigeIntro from '../components/PrestigeIntro'`
   - `import TelegramShare from '../components/TelegramShare'`

2. **ShotExplainer** (overlay modal — ONB-02):
   - Add state: `const [showShotExplainer, setShowShotExplainer] = useState(false)`
   - Add useEffect that runs on mount (or when match results are available):
     - Check if SHOT rewards > 0 in match results (find where SHOT reward amount is available in the component's props/state)
     - Check `localStorage.getItem('solshot_shot_explained')` is null
     - If both true: `setTimeout(() => setShowShotExplainer(true), 500)` (slight delay so results render first)
     - If either false: do nothing
   - On close handler: `localStorage.setItem('solshot_shot_explained', 'true'); setShowShotExplainer(false)`
   - Render: `<ShotExplainer isOpen={showShotExplainer} onClose={handleShotExplainerClose} />`
   - Place render OUTSIDE the tab structure — it's an overlay, not tab content

3. **PrestigeIntro** (in Progress tab — ONB-03):
   - Find the Progress tab content area (second tab)
   - Add PrestigeIntro at the BOTTOM of the Progress tab content
   - Pass props from available match/player data:
     - `currentTier`: player's prestige tier (check what's available in component state/props)
     - `shotBalance`: player's SHOT balance (check props/context)
     - `matchesPlayed`: either from props or use localStorage counter approach:
       - In the mount useEffect, increment: `localStorage.setItem('solshot_matches_played', String((parseInt(localStorage.getItem('solshot_matches_played') || '0')) + 1))`
       - Read the count for the prop
     - `onNavigatePrestige`: Pass `onNavigatePrestige={() => navigate('prestige')}` — the project uses prop-drilled `navigate` callback (NOT React Router). Find the existing `navigate` function in the component's props or destructured parameters and use it.
   - PrestigeIntro should NOT show if ShotExplainer is showing (add condition: `!showShotExplainer`)
   - Wrap PrestigeIntro render in the non-showing-explainer condition

4. **TelegramShare** (in Action tab — MOB-02):
   - Find the Action tab content area (third tab)
   - Find the existing X/Twitter share button
   - Add TelegramShare NEXT TO it in a flex row
   - If share buttons aren't already in a flex container, wrap them in one: `<div style={{display: 'flex', gap: '8px', justifyContent: 'center'}}>`
   - **Score props from roundWins:** The post-match screens have access to a `roundWins` object keyed by socket ID. Derive scores as follows:
     - `playerScore`: `roundWins[myId] || 0` (where `myId` is the local player's socket ID)
     - `opponentScore`: Get the OTHER key from `roundWins` — iterate `Object.keys(roundWins)` and find the key that is NOT `myId`, then use `roundWins[thatKey] || 0`
     - These represent rounds won, not HP. The share text will read like "3-1" (rounds won context).
   - Pass: `isWinner={true}`, `playerScore={roundWins[myId] || 0}`, `opponentScore={roundWins[opponentId] || 0}`

**B. LoseScreen.js integration — same pattern as WinScreen with these differences:**

1. Same three imports
2. ShotExplainer: Same logic (losers may also earn SHOT — check if SHOT rewards exist for losers)
3. PrestigeIntro: Same logic, same Progress tab placement. Same `onNavigatePrestige={() => navigate('prestige')}` pattern.
4. TelegramShare: `isWinner={false}`, same roundWins score derivation:
   - `playerScore={roundWins[myId] || 0}` (the loser's rounds won)
   - `opponentScore={roundWins[opponentId] || 0}` (the winner's rounds won)
   - Derive `opponentId` the same way: `Object.keys(roundWins).find(id => id !== myId)`
5. Matches played counter: Same localStorage increment (both win and lose count as a match played)

**Important:**
- Do NOT modify the tab structure (Result / Progress / Action)
- Do NOT change existing X/Twitter share functionality
- ShotExplainer is an OVERLAY — renders outside tabs, above everything
- PrestigeIntro is INLINE — renders inside Progress tab content
- TelegramShare is INLINE — renders next to existing share button in Action tab
- Use string concatenation in JSX
- Check useCallback dependency arrays if adding new state variables
  </action>
  <verify>
    - WinScreen.js imports and renders all three components
    - LoseScreen.js imports and renders all three components
    - ShotExplainer shows as overlay when SHOT earned for first time
    - ShotExplainer sets localStorage on dismiss (never shows again)
    - PrestigeIntro appears in Progress tab after 3+ matches (when conditions met)
    - PrestigeIntro receives `onNavigatePrestige={() => navigate('prestige')}` callback
    - PrestigeIntro does NOT show simultaneously with ShotExplainer
    - TelegramShare receives playerScore from `roundWins[myId]` and opponentScore from the other roundWins key
    - TelegramShare appears in Action tab next to X/Twitter share button
    - Tab structure (Result/Progress/Action) preserved
    - localStorage counter increments on each match completion
    - `npm run build` in client/ succeeds without errors
  </verify>
  <done>Post-match screens integrate all three new features: (1) one-time SHOT explainer overlay on first token earn, (2) prestige nudge in Progress tab after 3+ matches with navigate('prestige') callback, (3) Telegram share button alongside existing X share with roundWins-derived scores. All are additive — no existing UI broken.</done>
</task>

</tasks>

<verification>
- `npm run build` in client/ completes without errors
- ShotExplainer modal appears once on first SHOT earn, then never again
- PrestigeIntro nudge appears in Progress tab after 3+ matches for base-tier players with SHOT
- PrestigeIntro "Learn More" calls navigate('prestige') via onNavigatePrestige callback
- PrestigeIntro and ShotExplainer never show simultaneously
- TelegramShare button appears in Action tab next to X/Twitter share
- TelegramShare uses roundWins[myId] for playerScore, opponent's roundWins for opponentScore
- Clicking Telegram button opens t.me/share with pre-filled match results (rounds won format)
- Post-match tab structure (Result/Progress/Action) unchanged
- All localStorage keys use 'solshot_' prefix
</verification>

<success_criteria>
- ONB-02: SHOT explainer modal triggers on first earn, explains token purpose, dismissed with one click, never reappears
- ONB-03: Prestige introduced contextually in Progress tab after 3+ matches for eligible players, with navigate('prestige') callback
- MOB-02: Telegram share button on post-match screen with pre-filled text including rounds-won result and solshot.gg link
- All features are additive — existing post-match UI untouched
</success_criteria>

<output>
After completion, create `.planning/phases/12-onboarding-mobile-polish/12-03-SUMMARY.md`
</output>
