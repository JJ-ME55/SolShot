---
phase: 23-client-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/screens/BattleScreen.js
autonomous: true

must_haves:
  truths:
    - "Battle HUD displays correct total pot for N-player matches (e.g., 4-player 0.1 SOL shows Pot: 0.40)"
    - "2-player matches still show correct pot (backward compat: 2 * wager)"
  artifacts:
    - path: "client/src/screens/BattleScreen.js"
      provides: "N-player potDisplay calculation"
      contains: "numPlayersInMatch|players.*length.*maxPlayers"
  key_links:
    - from: "BattleScreen.js useEffect (startBattle)"
      to: "bridge.updateState({ potDisplay: wager * numPlayersInMatch })"
      via: "N-player multiplier from screenData"
      pattern: "wager.*numPlayersInMatch"
---

<objective>
Fix BattleScreen potDisplay to use N-player pot math instead of hardcoded `wager * 2`.

Purpose: During wagered matches, the battle HUD displays the total pot. With N-player support, a 4-player 0.1 SOL match should show "Pot: 0.40" not "Pot: 0.20". BattleHUD.js and PotDisplay.js already render `potDisplay` correctly from gameState — only the BattleScreen initialization needs the fix.

Output: Updated BattleScreen.js with correct N-player pot calculation.
</objective>

<execution_context>
@C:\Users\johnk\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\johnk\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-client-ux/23-RESEARCH.md
@client/src/screens/BattleScreen.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix potDisplay N-player calculation in BattleScreen</name>
  <files>client/src/screens/BattleScreen.js</files>
  <action>
In the `useEffect` that initializes the Phaser scene (the one containing `bridge.updateState` and `startBattle`), find this line:

```javascript
potDisplay: wager * 2,
```

Replace the hardcoded `* 2` with an N-player calculation. Add a `numPlayersInMatch` variable before the `bridge.updateState` call:

```javascript
const numPlayersInMatch = screenData?.players?.length || screenData?.maxPlayers || 2;
```

Then update the line:

```javascript
potDisplay: wager * numPlayersInMatch,
```

The fallback chain is defensive:
- `screenData.players.length` — the actual player array passed from ShopScreen
- `screenData.maxPlayers` — the room's max player setting
- `2` — safe default preserving 2-player backward compat

No other changes needed in BattleScreen.js. BattleHUD.js reads `potDisplay` from `gameState` and passes it to `PotDisplay`. PotDisplay renders `pot.toFixed(2)`. Both are already correct.
  </action>
  <verify>
1. Run `cd C:/Users/johnk/SolShot/client && npx react-scripts build 2>&1 | tail -5` — build must succeed.
2. Run `grep -n "potDisplay" client/src/screens/BattleScreen.js` — should show `wager * numPlayersInMatch` (not `wager * 2`).
3. Run `grep -n "numPlayersInMatch" client/src/screens/BattleScreen.js` — should show the variable declaration and usage.
4. Run `grep "wager \* 2" client/src/screens/BattleScreen.js` — should return no results.
  </verify>
  <done>
potDisplay uses `wager * numPlayersInMatch` where numPlayersInMatch is derived from screenData.players.length with maxPlayers and 2 as fallbacks. A 4-player 0.1 SOL match will display "Pot: 0.40" in the battle HUD. 2-player matches still show correct pot via the fallback. Build passes.
  </done>
</task>

</tasks>

<verification>
After task completes:

1. `cd client && npx react-scripts build` succeeds with zero errors
2. `grep "wager \* 2" client/src/screens/BattleScreen.js` returns no results
3. `grep "numPlayersInMatch" client/src/screens/BattleScreen.js` returns the declaration and the potDisplay usage
</verification>

<success_criteria>
- CLT-06 satisfied: pot display uses wager * numPlayers
- 2-player backward compat preserved via fallback chain
- No other BattleScreen changes (BattleHUD + PotDisplay already correct)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/23-client-ux/23-02-SUMMARY.md`
</output>
